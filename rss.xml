<?xml version="1.0" encoding="ISO-8859-1"?>
<rss version="2.0">
<channel>
<title></> FullStack Developer</title>
<link>https://sraban.github.io</link>
<description>Sraban Pahadasingh @Full Stack Developer: PHP, CodeIgniter, LARAVEL, ANGULAR 6-7-8, React Js, Ionic.</description>
<language>en-us</language>
<item>
<title>Newly added HTML features</title>
<description><![CDATA[<p>Here’s a list of some of the latest HTML features as documented by MDN Web Docs. These features enhance functionality, improve performance, and provide more flexibility for modern web development.</p>
<h3>1. <strong><code>&lt;dialog&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Represents a dialog box or other interactive component such as a modal.</li>
<li><strong>Features:</strong> Supports methods like <code>.showModal()</code> and <code>.close()</code>.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;dialog id="myDialog"&gt;Hello, I am a dialog!&lt;/dialog&gt;
    &lt;button onclick="document.getElementById('myDialog').showModal()"&gt;Open Dialog&lt;/button&gt;
</code></pre>
</li>
</ul>
<h3>2. <strong><code>&lt;template&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Holds client-side content that is not to be rendered when the page loads but can be used later via JavaScript.</li>
<li><strong>Features:</strong> Ideal for reusable code blocks.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;template id="myTemplate"&gt;
      &lt;p&gt;Template content goes here.&lt;/p&gt;
    &lt;/template&gt;
    &lt;script&gt;
      const template = document.getElementById('myTemplate').content.cloneNode(true);
      document.body.appendChild(template);
    &lt;/script&gt;
</code></pre>
</li>
</ul>
<h3>3. <strong><code>&lt;picture&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Provides an easy way to offer alternative versions of an image for different device displays.</li>
<li><strong>Features:</strong> Works with <code>srcset</code> and <code>media</code> attributes for responsive images.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;picture&gt;
      &lt;source srcset="image.webp" type="image/webp"&gt;
      &lt;source srcset="image.jpg" type="image/jpeg"&gt;
      &lt;img src="image.jpg" alt="A descriptive text"&gt;
    &lt;/picture&gt;
</code></pre>
</li>
</ul>
<h3>4. <strong><code>&lt;input type="color"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Allows users to select a color, offering a color picker interface.</li>
<li><strong>Features:</strong> Provides a default color input UI.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="favcolor"&gt;Choose a color:&lt;/label&gt;
    &lt;input type="color" id="favcolor" name="favcolor" value="#ff0000"&gt;
</code></pre>
</li>
</ul>
<h3>5. <strong><code>&lt;input type="date"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Provides a date picker, allowing users to select a date.</li>
<li><strong>Features:</strong> No need for JavaScript libraries for basic date picking.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="birthday"&gt;Select your birthday:&lt;/label&gt;
    &lt;input type="date" id="birthday" name="birthday"&gt;
</code></pre>
</li>
</ul>
<h3>6. <strong><code>&lt;details&gt;</code> and <code>&lt;summary&gt;</code> Elements</strong></h3>
<ul>
<li><strong>Description:</strong> Allows the creation of collapsible content.</li>
<li><strong>Features:</strong> <code>&lt;summary&gt;</code> defines the visible heading, while <code>&lt;details&gt;</code> contains the collapsible content.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;details&gt;
      &lt;summary&gt;More info&lt;/summary&gt;
      &lt;p&gt;Hidden content here.&lt;/p&gt;
    &lt;/details&gt;
</code></pre>
</li>
</ul>
<h3>7. <strong><code>&lt;datalist&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Provides a list of pre-defined options for an <code>&lt;input&gt;</code> element.</li>
<li><strong>Features:</strong> Enhances user input with suggestions.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="browser"&gt;Choose your browser:&lt;/label&gt;
    &lt;input list="browsers" id="browser" name="browser"&gt;
    &lt;datalist id="browsers"&gt;
      &lt;option value="Chrome"&gt;
      &lt;option value="Firefox"&gt;
      &lt;option value="Safari"&gt;
      &lt;option value="Edge"&gt;
    &lt;/datalist&gt;
</code></pre>
</li>
</ul>
<h3>8. <strong><code>&lt;input type="range"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Creates a slider control for numeric input.</li>
<li><strong>Features:</strong> Allows users to specify a numeric value within a given range.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="volume"&gt;Volume:&lt;/label&gt;
    &lt;input type="range" id="volume" name="volume" min="0" max="100"&gt;
</code></pre>
</li>
</ul>
<h3>9. <strong><code>&lt;output&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Represents the result of a calculation or user action.</li>
<li><strong>Features:</strong> Ideal for displaying calculated results.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="a"&gt;Value A:&lt;/label&gt;
    &lt;input id="a" type="number" value="10"&gt;
    &lt;label for="b"&gt;Value B:&lt;/label&gt;
    &lt;input id="b" type="number" value="20"&gt;
    &lt;button onclick="document.querySelector('output').value = parseInt(a.value) + parseInt(b.value)"&gt;Calculate&lt;/button&gt;
    &lt;output&gt;&lt;/output&gt;
</code></pre>
</li>
</ul>
<h3>10. <strong><code>&lt;progress&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Represents the completion progress of a task.</li>
<li><strong>Features:</strong> Displays a progress bar.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="file"&gt;Uploading file:&lt;/label&gt;
    &lt;progress id="file" value="32" max="100"&gt;32%&lt;/progress&gt;
</code></pre>
</li>
</ul>
<h3>11. <strong><code>&lt;meter&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Represents a scalar measurement within a known range, or a fractional value.</li>
<li><strong>Features:</strong> Displays a gauge.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="diskUsage"&gt;Disk Usage:&lt;/label&gt;
    &lt;meter id="diskUsage" value="0.6" min="0" max="1"&gt;60%&lt;/meter&gt;
</code></pre>
</li>
</ul>
<h3>12. <strong><code>&lt;input type="email"&gt;</code> and <code>&lt;input type="url"&gt;</code> Elements</strong></h3>
<ul>
<li><strong>Description:</strong> Validate email and URL input automatically.</li>
<li><strong>Features:</strong> Provides built-in validation.</li>
<li>
<p><strong>Examples:</strong></p>
<pre><code class="language-html">
    &lt;label for="email"&gt;Email:&lt;/label&gt;
    &lt;input type="email" id="email" name="email"&gt;

    &lt;label for="website"&gt;Website:&lt;/label&gt;
    &lt;input type="url" id="website" name="website"&gt;
</code></pre>
</li>
</ul>
<h3>13. <strong><code>&lt;input type="file" multiple&gt;</code> Attribute</strong></h3>
<ul>
<li><strong>Description:</strong> Allows the selection of multiple files.</li>
<li><strong>Features:</strong> Simplifies file uploads.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="files"&gt;Select files:&lt;/label&gt;
    &lt;input type="file" id="files" name="files" multiple&gt;
</code></pre>
</li>
</ul>
<h3>14. <strong><code>&lt;input type="search"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Provides a search input field with an optional clear button.</li>
<li><strong>Features:</strong> Enhances user experience for search inputs.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="search"&gt;Search:&lt;/label&gt;
    &lt;input type="search" id="search" name="search"&gt;
</code></pre>
</li>
</ul>
<h3>15. <strong><code>&lt;input type="number"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Allows numeric input with optional min, max, and step attributes.</li>
<li><strong>Features:</strong> Provides a spinner control.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="quantity"&gt;Quantity:&lt;/label&gt;
    &lt;input type="number" id="quantity" name="quantity" min="1" max="10" step="1"&gt;
</code></pre>
</li>
</ul>
<h3>16. <strong><code>&lt;input type="tel"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Provides an input field for telephone numbers.</li>
<li><strong>Features:</strong> Offers a specialized keyboard on mobile devices.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="phone"&gt;Phone number:&lt;/label&gt;
    &lt;input type="tel" id="phone" name="phone"&gt;
</code></pre>
</li>
</ul>
<h3>17. <strong><code>&lt;input type="datetime-local"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Combines date and time input.</li>
<li><strong>Features:</strong> Provides a date-time picker UI.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="appointment"&gt;Appointment:&lt;/label&gt;
    &lt;input type="datetime-local" id="appointment" name="appointment"&gt;
</code></pre>
</li>
</ul>
<h3>18. <strong><code>&lt;input type="week"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Allows the selection of a specific week.</li>
<li><strong>Features:</strong> Provides a week picker UI.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="week"&gt;Select week:&lt;/label&gt;
    &lt;input type="week" id="week" name="week"&gt;
</code></pre>
</li>
</ul>
<h3>19. <strong><code>&lt;input type="month"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Allows the selection of a specific month and year.</li>
<li><strong>Features:</strong> Provides a month picker UI.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="month"&gt;Select month:&lt;/label&gt;
    &lt;input type="month" id="month" name="month"&gt;
</code></pre>
</li>
</ul>
<h3>20. <strong><code>&lt;input type="time"&gt;</code> Element</strong></h3>
<ul>
<li><strong>Description:</strong> Allows the selection of a time without a date.</li>
<li><strong>Features:</strong> Provides a time picker UI.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="time"&gt;Select time:&lt;/label&gt;
    &lt;input type="time" id="time" name="time"&gt;
</code></pre>
</li>
</ul>
<h3>21. <strong><code>&lt;input type="file" accept="image/\*"&gt;</code> Attribute</strong></h3>
<ul>
<li><strong>Description:</strong> Limits file selection to specific file types.</li>
<li><strong>Features:</strong> Offers file type filtering.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="avatar"&gt;Select an image:&lt;/label&gt;
    &lt;input type="file" id="avatar" name="avatar" accept="image/*"&gt;
</code></pre>
</li>
</ul>
<h3>22. <strong><code>&lt;input type="password" autocomplete="new-password"&gt;</code> Attribute</strong></h3>
<ul>
<li><strong>Description:</strong> Improves security by preventing the browser from auto-filling passwords.</li>
<li><strong>Features:</strong> Encourages strong password practices.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-html">
    &lt;label for="password"&gt;Password:&lt;/label&gt;
    &lt;input type="password" id="password" name="password"  autocomplete="new-password"&gt;
</code></pre>
</li>
</ul>]]></description>
<link>https://sraban.github.io/post/newly-added-html-features</link>
<pubDate>Tue, 25 Jun 2024 02:03:00 +0500</pubDate>
</item>
<item>
<title>Common Errors in JS and from Rest API</title>
<description><![CDATA[<p>In JavaScript, errors can be categorized into different types, including logical errors, runtime errors, and syntax errors. Logical errors occur when the code does not behave as intended, often due to a mistake in the code logic. There are many other common errors that you might encounter or need to handle in JavaScript.</p>
<h2>List of JS Errors:</h2>
<h4>1. <strong>Division by Zero</strong></h4>
<ul>
<li><strong>Description:</strong> Attempting to divide a number by zero, which is mathematically undefined. In JavaScript, this results in <code>Infinity</code> for positive numbers and <code>-Infinity</code> for negative numbers.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let result = 10 / 0;  // Result is Infinity
</code></pre>
</li>
</ul>
<h4>2. <strong>Null or Undefined Reference</strong></h4>
<ul>
<li><strong>Description:</strong> Trying to access a property or call a method on <code>null</code> or <code>undefined</code>.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let obj = null;
    console.log(obj.property);  // Throws TypeError: Cannot read property 'property' of null
</code></pre>
</li>
</ul>
<h4>3. <strong>NaN (Not a Number)</strong></h4>
<ul>
<li><strong>Description:</strong> Using a mathematical operation that does not produce a meaningful number, resulting in <code>NaN</code>.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let result = "string" / 2;  // Result is NaN
</code></pre>
</li>
</ul>
<h4>4. <strong>Array Out of Bounds</strong></h4>
<ul>
<li><strong>Description:</strong> Accessing an array element with an index that is outside the array's range.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let array = [1, 2, 3];
    console.log(array[5]);  // Result is undefined
</code></pre>
</li>
</ul>
<h4>5. <strong>Invalid JSON Parsing</strong></h4>
<ul>
<li><strong>Description:</strong> Attempting to parse a string as JSON when it is not properly formatted.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let jsonString = "{ invalid JSON }";
    JSON.parse(jsonString);  // Throws SyntaxError: Unexpected token i in JSON at position 2
</code></pre>
</li>
</ul>
<h4>6. <strong>Division by Zero in Modulo Operation</strong></h4>
<ul>
<li><strong>Description:</strong> Using zero as the divisor in a modulo operation.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let result = 10 % 0;  // Result is NaN
</code></pre>
</li>
</ul>
<h4>7. <strong>Negative Square Root</strong></h4>
<ul>
<li><strong>Description:</strong> Calculating the square root of a negative number, which is not a real number in real number space.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let result = Math.sqrt(-1);  // Result is NaN
</code></pre>
</li>
</ul>
<h4>8. <strong>Exponential Overflow</strong></h4>
<ul>
<li><strong>Description:</strong> Exceeding the upper limit of the number range that JavaScript can handle.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let result = Math.pow(10, 1000);  // Result is Infinity
</code></pre>
</li>
</ul>
<h4>9. <strong>Function Argument Count Mismatch</strong></h4>
<ul>
<li><strong>Description:</strong> Passing an incorrect number of arguments to a function, which can lead to unexpected results.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    function add(a, b) {
      return a + b;
    }
    let result = add(1);  // Result is NaN because b is undefined
</code></pre>
</li>
</ul>
<h4>10. <strong>Circular JSON Structure</strong></h4>
<ul>
<li><strong>Description:</strong> Attempting to stringify an object with circular references, which is not possible.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let obj = {};
    obj.self = obj;
    JSON.stringify(obj);  // Throws TypeError: Converting circular structure to JSON
</code></pre>
</li>
</ul>
<h4>11. <strong>Array Length Modification</strong></h4>
<ul>
<li><strong>Description:</strong> Changing the length of an array manually can cause truncation or add undefined elements.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let array = [1, 2, 3];
    array.length = 5;
    console.log(array);  // Result is [1, 2, 3, undefined, undefined]
</code></pre>
</li>
</ul>
<h4>12. <strong>Invalid Function Constructor</strong></h4>
<ul>
<li><strong>Description:</strong> Passing incorrect or malicious code to the <code>Function</code> constructor.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let func = new Function('console.log("hello');  // Throws SyntaxError: Unexpected end of input
</code></pre>
</li>
</ul>
<h4>13. <strong>Range Error</strong></h4>
<ul>
<li><strong>Description:</strong> Using a number outside the allowable range for a specific function.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let result = new Array(-1);  // Throws RangeError: Invalid array length
</code></pre>
</li>
</ul>
<h4>14. <strong>Type Error</strong></h4>
<ul>
<li><strong>Description:</strong> Performing an operation on a value that is not of the expected type.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let num = 123;
    num.toUpperCase();  // Throws TypeError: num.toUpperCase is not a function
</code></pre>
</li>
</ul>
<h4>15. <strong>Stack Overflow</strong></h4>
<ul>
<li><strong>Description:</strong> Caused by excessive recursion, leading to the call stack exceeding its limit.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    function recursive() {
      recursive();
    }
    recursive();  // Throws RangeError: Maximum call stack size exceeded
</code></pre>
</li>
</ul>
<h4>16. <strong>Invalid Array Index</strong></h4>
<ul>
<li><strong>Description:</strong> Using a non-integer or negative index to access an array element.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let array = [1, 2, 3];
    console.log(array[-1]);  // Result is undefined
</code></pre>
</li>
</ul>
<h4>17. <strong>Invalid Date</strong></h4>
<ul>
<li><strong>Description:</strong> Creating a <code>Date</code> object with invalid date values.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-javascript">
    let date = new Date('invalid date');  // Result is Invalid Date
</code></pre>
</li>
</ul>
<h4>Conclusion</h4>
<p>Understanding these common errors can help you write more robust JavaScript code and handle edge cases effectively. Proper error handling and validation can prevent many of these issues from occurring, improving both the stability and user experience of your applications.</p>
<h2>List of Rest API Errors</h2>
<p>REST API errors can occur due to various reasons ranging from client errors to server issues. Properly handling and returning meaningful error responses in a REST API can significantly improve the developer experience and make debugging easier. Here’s a list of common REST API errors along with descriptions and examples.</p>
<h4>1. <strong>400 Bad Request</strong></h4>
<ul>
<li><strong>Description:</strong> The server cannot process the request due to client error (e.g., malformed request syntax, invalid request message framing, or deceptive request routing).</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Bad Request",
      "message": "Invalid JSON format in request body."
    }
</code></pre>
</li>
</ul>
<h4>2. <strong>401 Unauthorized</strong></h4>
<ul>
<li><strong>Description:</strong> The client must authenticate itself to get the requested response. This is typically used when authentication is required and has failed or has not yet been provided.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Unauthorized",
      "message": "Authentication is required to access this resource."
    }
</code></pre>
</li>
</ul>
<h4>3. <strong>403 Forbidden</strong></h4>
<ul>
<li><strong>Description:</strong> The server understands the request but refuses to authorize it. This status is used when the client does not have the necessary permissions for the resource.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Forbidden",
      "message": "You do not have permission to access this resource."
    }
</code></pre>
</li>
</ul>
<h4>4. <strong>404 Not Found</strong></h4>
<ul>
<li><strong>Description:</strong> The server cannot find the requested resource. This status is typically used when the endpoint is valid but the resource itself does not exist.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Not Found",
      "message": "The requested resource could not be found."
    }
</code></pre>
</li>
</ul>
<h4>5. <strong>405 Method Not Allowed</strong></h4>
<ul>
<li><strong>Description:</strong> The request method is not supported for the requested resource. For example, an API might support <code>GET</code> but not <code>POST</code> for a specific endpoint.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Method Not Allowed",
      "message": "The requested HTTP method is not allowed for this endpoint."
    }
</code></pre>
</li>
</ul>
<h4>6. <strong>406 Not Acceptable</strong></h4>
<ul>
<li><strong>Description:</strong> The requested resource is capable of generating only content not acceptable according to the Accept headers sent in the request.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Not Acceptable",
      "message": "The requested content type is not supported by this resource."
    }
</code></pre>
</li>
</ul>
<h4>7. <strong>409 Conflict</strong></h4>
<ul>
<li><strong>Description:</strong> The request could not be processed because of conflict in the request, such as an edit conflict between multiple simultaneous updates.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Conflict",
      "message": "The request could not be completed due to a conflict with the current state of the resource."
    }
</code></pre>
</li>
</ul>
<h4>8. <strong>410 Gone</strong></h4>
<ul>
<li><strong>Description:</strong> The requested resource is no longer available and has been permanently removed.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Gone",
      "message": "The requested resource is no longer available and will not be available again."
    }
</code></pre>
</li>
</ul>
<h4>9. <strong>415 Unsupported Media Type</strong></h4>
<ul>
<li><strong>Description:</strong> The request entity has a media type which the server or resource does not support. For example, the client uploads an image in a format not supported by the server.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Unsupported Media Type",
      "message": "The requested media type is not supported."
    }
</code></pre>
</li>
</ul>
<h4>10. <strong>422 Unprocessable Entity</strong></h4>
<ul>
<li><strong>Description:</strong> The request was well-formed but was unable to be followed due to semantic errors. This is commonly used for validation errors in APIs.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Unprocessable Entity",
      "message": "Validation failed for one or more fields.",
      "details": {
        "field": "username",
        "error": "Username already exists."
      }
    }
</code></pre>
</li>
</ul>
<h4>11. <strong>429 Too Many Requests</strong></h4>
<ul>
<li><strong>Description:</strong> The user has sent too many requests in a given amount of time (&quot;rate limiting&quot;).</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Too Many Requests",
      "message": "You have exceeded the rate limit. Please try again later."
    }
</code></pre>
</li>
</ul>
<h4>12. <strong>500 Internal Server Error</strong></h4>
<ul>
<li><strong>Description:</strong> The server encountered a situation it doesn't know how to handle. This is a generic error message for unexpected conditions.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Internal Server Error",
      "message": "An unexpected error occurred on the server."
    }
</code></pre>
</li>
</ul>
<h4>13. <strong>502 Bad Gateway</strong></h4>
<ul>
<li><strong>Description:</strong> The server, while acting as a gateway or proxy, received an invalid response from the upstream server it accessed in attempting to fulfill the request.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Bad Gateway",
      "message": "The server received an invalid response from the upstream server."
    }
</code></pre>
</li>
</ul>
<h4>14. <strong>503 Service Unavailable</strong></h4>
<ul>
<li><strong>Description:</strong> The server is not ready to handle the request. Common causes are a server that is down for maintenance or that is overloaded.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Service Unavailable",
      "message": "The service is temporarily unavailable. Please try again later."
    }
</code></pre>
</li>
</ul>
<h4>15. <strong>504 Gateway Timeout</strong></h4>
<ul>
<li><strong>Description:</strong> The server, while acting as a gateway or proxy, did not receive a timely response from the upstream server specified by the URI.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Gateway Timeout",
      "message": "The server did not receive a timely response from the upstream server."
    }
</code></pre>
</li>
</ul>
<h4>16. <strong>502 Invalid Token</strong></h4>
<ul>
<li><strong>Description:</strong> The token provided in the request is invalid, expired, or missing.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Invalid Token",
      "message": "The token provided is either invalid or has expired. Please authenticate again."
    }
</code></pre>
</li>
</ul>
<h4>17. <strong>404 Resource Not Found</strong></h4>
<ul>
<li><strong>Description:</strong> The requested resource could not be found. This is similar to a generic 404 error but more specific to API resources.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Resource Not Found",
      "message": "The specified resource was not found."
    }
</code></pre>
</li>
</ul>
<h4>18. <strong>400 Missing Parameters</strong></h4>
<ul>
<li><strong>Description:</strong> Required parameters for the request are missing or incomplete.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Missing Parameters",
      "message": "Required parameters are missing from the request."
    }
</code></pre>
</li>
</ul>
<h4>19. <strong>405 Rate Limit Exceeded</strong></h4>
<ul>
<li><strong>Description:</strong> The client has exceeded the number of requests allowed in a given period.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Rate Limit Exceeded",
      "message": "You have made too many requests in a short period of time. Please slow down."
    }
</code></pre>
</li>
</ul>
<h4>20. <strong>403 Insufficient Permissions</strong></h4>
<ul>
<li><strong>Description:</strong> The client does not have the required permissions to access the resource.</li>
<li>
<p><strong>Example:</strong></p>
<pre><code class="language-json">
    {
      "error": "Insufficient Permissions",
      "message": "You do not have the necessary permissions to access this resource."
    }
</code></pre>
</li>
</ul>]]></description>
<link>https://sraban.github.io/post/common-errors-in-js-and-from-rest-api</link>
<pubDate>Tue, 25 Jun 2024 01:49:00 +0500</pubDate>
</item>
<item>
<title>ngRx Features</title>
<description><![CDATA[<p>NgRx (pronounced &quot;N-G-R-X&quot;) is a powerful library for managing state in Angular applications using reactive programming principles. It is built on top of <a href="https://rxjs.dev/">RxJS</a>, which provides a way to compose asynchronous and event-based programs using observable sequences. NgRx follows the Redux pattern, a predictable state container for JavaScript applications, making it easier to manage and debug the state of your application. Here’s a detailed breakdown of NgRx and its core concepts:</p>
<p>Imagine NgRx as a well-organized factory. Each part of the factory represents a core concept:</p>
<ul>
<li><strong>Store</strong>: This is the warehouse where all the finished products (state) are stored. It's a single, central location where everything is neatly organized and immutable.</li>
<li><strong>Actions</strong>: These are the orders or requests that come into the factory, specifying what products (state changes) need to be made.</li>
<li><strong>Reducers</strong>: These are the machines in the factory that process raw materials (current state and actions) and produce new products (new state).</li>
<li><strong>Selectors</strong>: These are the quality inspectors that check the products (state) and pick out the parts that are needed.</li>
<li><strong>Effects</strong>: These are the logistic operations that handle external interactions like fetching raw materials (data fetching) and ensure that everything is running smoothly.</li>
</ul>
<h3>Core Concepts of NgRx</h3>
<ol>
<li>
<p><strong>Store</strong></p>
<ul>
<li>The <strong>Store</strong> is a single source of truth that holds the state of your application. It is an immutable object tree in which the state is represented as a plain JavaScript object. You can think of it as a database that holds all the state of your application.</li>
<li>Example:</li>
</ul>
<pre><code class="language-typescript">
    interface AppState {
     counter: number;
    }
</code></pre>
</li>
<li>
<p><strong>Actions</strong></p>
<ul>
<li><strong>Actions</strong> are payloads of information that send data from your application to your store. They are the only source of information for the store and follow a strict structure with a <code>type</code> field that describes the action being performed and an optional <code>payload</code>.</li>
<li>Example:</li>
</ul>
<pre><code class="language-typescript">
    export const increment = createAction('[Counter] Increment');
    export const decrement = createAction('[Counter] Decrement');
    export const reset = createAction('[Counter] Reset');
</code></pre>
</li>
<li>
<p><strong>Reducers</strong></p>
<ul>
<li><strong>Reducers</strong> are pure functions that determine how the state changes in response to an action. They take the current state and an action as arguments and return a new state.</li>
<li>Example:</li>
</ul>
<pre><code class="language-typescript">
    const initialState: AppState = { counter: 0 };

    const counterReducer = createReducer(
     initialState,
     on(increment, state =&gt; ({ counter: state.counter + 1 })),
     on(decrement, state =&gt; ({ counter: state.counter - 1 })),
     on(reset, state =&gt; ({ counter: 0 }))
    );
</code></pre>
</li>
<li>
<p><strong>Selectors</strong></p>
<ul>
<li><strong>Selectors</strong> are pure functions used for obtaining slices of the state from the store. They provide a way to access parts of the state and are memoized to improve performance.</li>
<li>Example:</li>
</ul>
<pre><code class="language-typescript">
    const selectCounter = (state: AppState) =&gt; state.counter;
</code></pre>
</li>
<li>
<p><strong>Effects</strong></p>
<ul>
<li><strong>Effects</strong> handle side effects in NgRx, such as fetching data from a server or logging actions. They listen for dispatched actions, perform operations, and then return new actions to the store.</li>
<li>Example:</li>
</ul>
<pre><code class="language-typescript">
    @Injectable()
    export class CounterEffects {
     loadCounters$ = createEffect(() =&gt;
       this.actions$.pipe(
         ofType(loadCounters),
         mergeMap(() =&gt;
           this.counterService.getAll().pipe(
             map(counters =&gt; loadCountersSuccess({ counters })),
             catchError(error =&gt; of(loadCountersFailure({ error })))
           )
         )
       )
     );

     constructor(
       private actions$: Actions,
       private counterService: CounterService
     ) {}
    }
</code></pre>
</li>
</ol>
<h3>Key Features of NgRx</h3>
<ol>
<li>
<p><strong>State Management</strong></p>
<ul>
<li>NgRx provides a robust state management solution that helps maintain a consistent state across different parts of the application. It encourages the use of a single source of truth and a unidirectional data flow, making state changes predictable and traceable.</li>
</ul>
</li>
<li>
<p><strong>Immutable State</strong></p>
<ul>
<li>The state in NgRx is immutable, meaning it cannot be changed directly. Instead, new states are created based on actions and the current state, which helps prevent accidental mutations and simplifies debugging.</li>
</ul>
</li>
<li>
<p><strong>Reactive Programming</strong></p>
<ul>
<li>NgRx leverages RxJS to handle asynchronous operations and state changes reactively. Observables and operators allow for powerful state management patterns and efficient handling of async data flows.</li>
</ul>
</li>
<li>
<p><strong>DevTools Integration</strong></p>
<ul>
<li>NgRx integrates with Redux DevTools, providing time-travel debugging and state inspection. This makes it easier to understand and debug the state changes in your application.</li>
</ul>
</li>
<li>
<p><strong>Modularity</strong></p>
<ul>
<li>NgRx encourages modular code organization by separating concerns through actions, reducers, selectors, and effects. This separation improves code maintainability and readability.</li>
</ul>
</li>
<li><strong>Side Effects Management</strong>
<ul>
<li>NgRx effects handle side effects such as API calls, ensuring that business logic and state management are decoupled and maintainable.</li>
</ul></li>
</ol>
<h3>Examples and Use Cases</h3>
<h4>Basic Counter Example</h4>
<ol>
<li>
<p><strong>Action Definitions</strong>:</p>
<pre><code class="language-typescript">
    export const increment = createAction('[Counter] Increment');
    export const decrement = createAction('[Counter] Decrement');
    export const reset = createAction('[Counter] Reset');
</code></pre>
</li>
<li>
<p><strong>Reducer</strong>:</p>
<pre><code class="language-typescript">
    const initialState: AppState = { counter: 0 };

    const counterReducer = createReducer(
     initialState,
     on(increment, state =&gt; ({ counter: state.counter + 1 })),
     on(decrement, state =&gt; ({ counter: state.counter - 1 })),
     on(reset, state =&gt; ({ counter: 0 }))
    );
</code></pre>
</li>
<li>
<p><strong>Selector</strong>:</p>
<pre><code class="language-typescript">
    const selectCounter = (state: AppState) =&gt; state.counter;
</code></pre>
</li>
<li>
<p><strong>Component Integration</strong>:</p>
<pre><code class="language-typescript">
    @Component({
     selector: 'app-counter',
     template: `
       &lt;div&gt;Current Count: {{ counter$ | async }}&lt;/div&gt;
       &lt;button (click)="increment()"&gt;Increment&lt;/button&gt;
       &lt;button (click)="decrement()"&gt;Decrement&lt;/button&gt;
       &lt;button (click)="reset()"&gt;Reset&lt;/button&gt;
     `
    })
    export class CounterComponent {
     counter$ = this.store.select(selectCounter);

     constructor(private store: Store&lt;AppState&gt;) {}

     increment() {
       this.store.dispatch(increment());
     }

     decrement() {
       this.store.dispatch(decrement());
     }

     reset() {
       this.store.dispatch(reset());
     }
    }
</code></pre>
</li>
</ol>
<h4><strong>Example:</strong></h4>
<p><img src="https://i.imgur.com/8D3Jjy9.png" alt="enter image description here" /></p>]]></description>
<link>https://sraban.github.io/post/ngrx-features</link>
<pubDate>Tue, 25 Jun 2024 01:24:00 +0500</pubDate>
</item>
<item>
<title>Typescript Features</title>
<description><![CDATA[<p>TypeScript concepts:</p>
<h3>01. Type Assertion</h3>
<p>Type assertion in TypeScript is a way to tell the compiler that you know more about the type of a value than TypeScript can infer automatically. It has two forms:</p>
<ul>
<li>
<p><strong>Angle-bracket syntax</strong>: <code>&lt;Type&gt;value</code></p>
<pre><code class="language-typescript">
  let myList = [1, 2, 3];
  let length1 = (&lt;number[]&gt;myList).length; // Using angle-bracket syntax
</code></pre>
</li>
<li>
<p><strong>As syntax</strong>: <code>value as Type</code></p>
<pre><code class="language-typescript">
  let length2 = (myList as number[]).length; // Using 'as' syntax
</code></pre>
</li>
</ul>
<p>Type assertion does not change the runtime behavior but helps TypeScript's type checker understand the intended type when the developer has more specific knowledge about it.</p>
<h3>02. TypeScript Generics</h3>
<p>Generics in TypeScript allow you to create reusable components that can work with a variety of data types while maintaining type safety. The syntax for generics involves using angle brackets (<code>&lt; &gt;</code>) to declare type parameters:</p>
<pre><code class="language-typescript">
    function myMethod&lt;T&gt;(arg: T): T {
      return arg;
    }

    let value = myMethod&lt;number&gt;(10); // Specify the type explicitly
    let anotherValue = myMethod("Hello"); // TypeScript infers the type
</code></pre>
<p>Generics can be used with interfaces, classes, functions, properties, and variables to create flexible and type-safe constructs.</p>
<h3>03. <code>typeof</code> and <code>keyof</code></h3>
<ul>
<li><strong><code>typeof</code></strong>: Returns the type of a variable, function, or object.</li>
</ul>
<pre><code class="language-typescript">
      let x = 10;
      type T = typeof x; // T is 'number'
</code></pre>
<ul>
<li><strong><code>keyof</code></strong>: Returns the union of keys of a given type.</li>
</ul>
<pre><code class="language-typescript">
    interface Person {
     name: string;
     age: number;
    }

    type Keys = keyof Person; // Keys is 'name' | 'age'
</code></pre>
<h3>04. Type Inference</h3>
<p>Type inference in TypeScript refers to the ability of the type system to deduce types in certain situations based on the context and initialization values, without requiring explicit type annotations:</p>
<pre><code class="language-typescript">
    let x = 10; // TypeScript infers 'number' type for x
    let y = "Hello"; // TypeScript infers 'string' type for y
</code></pre>
<p>Type inference reduces the need for manual type annotations, making code more concise while maintaining type safety.</p>
<h3>05. Type Aliases</h3>
<p>Type aliases in TypeScript allow you to create new names for existing types. They are particularly useful for defining complex types or for simplifying type annotations:</p>
<pre><code class="language-typescript">
    type Container&lt;T&gt; = { value: T };

    let numContainer: Container&lt;number&gt; = { value: 10 };
    let strContainer: Container&lt;string&gt; = { value: "Hello" };
</code></pre>
<p>Type aliases improve code readability and maintainability by providing descriptive names for types.</p>
<h3>06. Conditional Types</h3>
<p>Conditional types in TypeScript enable the creation of types that depend on the relationships between types:</p>
<pre><code class="language-typescript">
    type TypeName&lt;T&gt; =
      T extends string ? "string" :
      T extends number ? "number" :
      T extends boolean ? "boolean" :
      "unknown";

    let strType: TypeName&lt;string&gt; = "string";
    let numType: TypeName&lt;number&gt; = "number";
</code></pre>
<p>Conditional types use the <code>extends</code> keyword to check if a type satisfies a condition and then return different types based on that condition (<code>X</code> if true, <code>Y</code> if false).</p>
<h3>07. Distributive Conditional Types</h3>
<p>Distributive conditional types in TypeScript automatically distribute over union types:</p>
<pre><code class="language-typescript">
    type BoxedValue&lt;T&gt; = { value: T };

    type BoxedNumber = BoxedValue&lt;number | string&gt;;
    // Equivalent to:
    // type BoxedNumber = BoxedValue&lt;number&gt; | BoxedValue&lt;string&gt;;
</code></pre>
<p>When a conditional type is applied to a union type, TypeScript applies the type condition to each constituent type of the union.</p>
<h3>08. Object-Oriented Principles supported by TypeScript</h3>
<p>TypeScript supports fundamental object-oriented principles such as:</p>
<ul>
<li>
<p><strong>Encapsulation</strong>: Bundling data and methods that operate on the data into a single unit (class).</p>
</li>
<li>
<p><strong>Inheritance</strong>: Creating new classes (derived classes) from existing ones (base classes) to reuse and extend functionality.</p>
</li>
<li><strong>Polymorphism</strong>: Using a single interface to represent multiple types (subtyping), allowing objects of different types to be treated as instances of a common superclass.</li>
</ul>
<h3>09. Awaited Type and Promise Improvements</h3>
<p>TypeScript 4.3 introduced the <code>Awaited</code> type to extract the type awaited by a Promise:</p>
<pre><code class="language-typescript">
    type Awaited&lt;T&gt; = T extends PromiseLike&lt;infer U&gt; ? U : T;

    async function fetchData(): Promise&lt;string&gt; {
      return "Data";
    }

    type Data = Awaited&lt;ReturnType&lt;typeof fetchData&gt;&gt;; // Data is 'string'
</code></pre>
<p>This improvement enhances type inference and provides better support for async functions and promise chains.</p>
<h3>10. Recursive Type Aliases</h3>
<p>Recursive type aliases in TypeScript allow defining types that reference themselves:</p>
<pre><code class="language-typescript">
    type TreeNode&lt;T&gt; = {
      value: T;
      left?: TreeNode&lt;T&gt;;
      right?: TreeNode&lt;T&gt;;
    };

    let node: TreeNode&lt;number&gt; = {
      value: 10,
      left: {
        value: 5
      }
    };
</code></pre>
<p>Recursive type aliases are useful for defining recursive data structures like trees or linked lists.</p>]]></description>
<link>https://sraban.github.io/post/typescript-features</link>
<pubDate>Tue, 25 Jun 2024 01:13:00 +0500</pubDate>
</item>
<item>
<title>JS API</title>
<description><![CDATA[<h4><strong><code>setTimeout()</code></strong></h4>
<p>Executes a function after a specified delay.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    setTimeout(() =&gt; {
      console.log('Executed after 1 second');
    }, 1000);
</code></pre>
<h4><strong><code>fetch()</code></strong></h4>
<p>Used for making network requests. It returns a <code>Promise</code> that resolves to the response.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    fetch('https://api.example.com/data')
      .then(response =&gt; response.json())
      .then(data =&gt; console.log(data));
</code></pre>
<h4><strong><code>Promise()</code></strong></h4>
<p>Represents the eventual completion (or failure) of an asynchronous operation and its resulting value.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    const promise = new Promise((resolve, reject) =&gt; {
      const success = true;
      if (success) {
        resolve('Operation successful');
      } else {
        reject('Operation failed');
      }
    });

    promise.then(result =&gt; console.log(result))
           .catch(error =&gt; console.log(error));
</code></pre>]]></description>
<link>https://sraban.github.io/post/js-api</link>
<pubDate>Tue, 25 Jun 2024 01:06:00 +0500</pubDate>
</item>
<item>
<title>Types of Errors, Error Codes in JavaScript</title>
<description><![CDATA[<p>JavaScript has several built-in error types that help in identifying and handling different kinds of errors. Here are some common error types:</p>
<h4><strong>SyntaxError</strong></h4>
<p>Occurs when there is a syntax issue in the code.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    console.log('Hello; // Missing closing quote
</code></pre>
<h4><strong>ReferenceError</strong></h4>
<p>Occurs when a non-existent variable is referenced.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    console.log(nonExistentVariable); // ReferenceError
</code></pre>
<h4><strong>TypeError</strong></h4>
<p>Occurs when a value is not of the expected type.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    null.foo(); // TypeError: Cannot read property 'foo' of null
</code></pre>
<h4><strong>RangeError</strong></h4>
<p>Occurs when a value is not within the set or acceptable range.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    new Array(-1); // RangeError: Invalid array length
</code></pre>
<h4><strong>EvalError</strong></h4>
<p>Occurs when the <code>eval()</code> function is used incorrectly. It's rarely encountered because <code>eval()</code> is discouraged.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    eval('foo)'); // SyntaxError: Unexpected token
</code></pre>
<h4><strong>URIError</strong></h4>
<p>Occurs when there is an error in encoding or decoding URIs.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    decodeURIComponent('%'); // URIError: URI malformed
</code></pre>
<h4><strong>Error Codes</strong></h4>
<p>Custom errors can be created with specific codes to handle specific conditions.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    class CustomError extends Error {
      constructor(message, code) {
        super(message);
        this.code = code;
      }
    }

    throw new CustomError('Custom error message', 'E_CUSTOM');
</code></pre>]]></description>
<link>https://sraban.github.io/post/types-of-errors-error-codes-in-javascript</link>
<pubDate>Tue, 25 Jun 2024 00:48:00 +0500</pubDate>
</item>
<item>
<title>.call(), .bind(), .apply() Differences in JS</title>
<description><![CDATA[<p>Certainly! Understanding the differences between <code>.call()</code>, <code>.apply()</code>, and <code>.bind()</code> is crucial for effective JavaScript development. They all serve the purpose of function invocation with a specific context (<code>this</code> value), but they differ in their usage and application.</p>
<h4><strong><code>.call()</code></strong></h4>
<p>Calls a function with a specified <code>this</code> value and individual arguments.</p>
<ul>
<li>Invokes the function immediately.</li>
<li>Allows you to pass arguments individually.</li>
<li>Useful when you need to call a function with a specific <code>this</code> value and arguments.</li>
</ul>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    function greet(greeting, name) {
      console.log(`${greeting}, ${name}`);
    }

    greet.call(null, 'Hello', 'Alice'); // Output: Hello, Alice
</code></pre>
<h4><strong><code>.apply()</code></strong></h4>
<p>Similar to <code>.call()</code>, but takes an array of arguments.</p>
<ul>
<li>Invokes the function immediately.</li>
<li>Allows you to pass arguments as an array.</li>
<li>
<p>Handy when the arguments are already in an array or array-like structure.</p>
</li>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
    greet.apply(null, ['Hello', 'Alice']); // Output: Hello, Alice
</code></pre>
<pre><code class="language-javascript">
    function showThis() {
      console.log(this);
    }

    showThis.call({ name: 'Bob' }); // Output: { name: 'Bob' }
    showThis.apply({ name: 'Bob' }); // Output: { name: 'Bob' }
</code></pre>
<h4><strong><code>.bind()</code></strong></h4>
<p>Creates a new function that, when called, has its <code>this</code> value set to the provided value, with a given sequence of arguments preceding any provided when the new function is called.</p>
<ul>
<li>Does not invoke the function immediately.</li>
<li>Creates a new function with a bound <code>this</code> value and optionally, pre-set arguments.</li>
<li>
<p>Useful for creating a new function that can be called later with the specific <code>this</code> context.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
</li>
</ul>
<pre><code class="language-javascript">
    const greetAlice = greet.bind(null, 'Hello', 'Alice');
    greetAlice(); // Output: Hello, Alice
</code></pre>
<pre><code class="language-javascript">
    const person = {
      name: 'Alice',
      showThis() {
        console.log(this);
      }
    };

    const boundShowThis = person.showThis.bind({ name: 'Bob' });
    boundShowThis(); // Output: { name: 'Bob' }
</code></pre>
<hr />
<h4><strong>Summary Table</strong></h4>
<table>
<thead>
<tr>
<th>Feature</th>
<th><code>.call()</code></th>
<th><code>.apply()</code></th>
<th><code>.bind()</code></th>
</tr>
</thead>
<tbody>
<tr>
<td>Invocation</td>
<td>Immediate</td>
<td>Immediate</td>
<td>Delayed (returns new function)</td>
</tr>
<tr>
<td>Arguments</td>
<td>Individual (arg1, arg2, ...)</td>
<td>Array ([arg1, arg2, ...])</td>
<td>Individual (arg1, arg2, ...)</td>
</tr>
<tr>
<td><code>this</code> Binding</td>
<td>Immediate, changes <code>this</code> context</td>
<td>Immediate, changes <code>this</code> context</td>
<td>Delayed, changes <code>this</code> context for the new function</td>
</tr>
<tr>
<td>Use Case</td>
<td>Borrowing methods, invoking with a different <code>this</code></td>
<td>Functions expecting arguments as arrays</td>
<td>Event handlers, preserving <code>this</code> context</td>
</tr>
</tbody>
</table>]]></description>
<link>https://sraban.github.io/post/call-bind-apply-differences-in-js</link>
<pubDate>Tue, 25 Jun 2024 00:23:00 +0500</pubDate>
</item>
<item>
<title>Event Loop, Call Stack, Callback Queue, Event Table, Mapping, Microtask</title>
<description><![CDATA[<h4><strong>1.Event Loop</strong></h4>
<p>The Event Loop is a fundamental concept in JavaScript that enables asynchronous programming. It allows non-blocking operations to be executed in a single-threaded environment, managing the execution of multiple operations over time.</p>
<ul>
<li><strong>How It Works</strong>: The Event Loop constantly checks the Call Stack to see if it's empty. If it is, it pushes the first callback from the Callback Queue (or Microtask Queue) onto the Call Stack to be executed.</li>
</ul>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/format:webp/1*5qxfasS_NYSn5nO4JFFNDQ.png" alt="Event Loop Diagram" title="Event Loop Diagram" /></p>
<h4><strong>2.Call Stack</strong></h4>
<p>The Call Stack is a data structure that keeps track of function calls. When a function is called, it is pushed onto the stack, and when the function completes, it is popped off the stack.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
      function greet() {
        console.log('Hello');
      }

      function sayGoodbye() {
        console.log('Goodbye');
      }

      greet(); // Pushed onto the Call Stack
      sayGoodbye(); // Pushed onto the Call Stack after greet() completes</code></pre>
<h4><strong>3.Callback Queue</strong></h4>
<p>The Callback Queue (or Task Queue) is a queue where callback functions are stored after being moved from the Event Table (or task scheduler).</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
      setTimeout(() =&gt; {
        console.log('This is a callback');
      }, 1000);
</code></pre>
<h4><strong>4.Event Table</strong></h4>
<p>The Event Table is a data structure that keeps track of events and their associated callback functions. When an event occurs, the corresponding callback function is moved to the Callback Queue.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
      document.addEventListener('click', () =&gt; {
        console.log('Document clicked');
      });
</code></pre>
<h4><strong>5.Microtasks</strong></h4>
<p>Microtasks are similar to tasks in the Callback Queue but have higher priority. Microtasks include operations like <code>Promise</code> callbacks and <code>MutationObserver</code> callbacks.</p>
<ul>
<li><strong>Example</strong>:</li>
</ul>
<pre><code class="language-javascript">
      Promise.resolve().then(() =&gt; {
        console.log('Microtask');
      });
</code></pre>
<h4><strong>6.Mapping</strong></h4>
<p>Mapping in the context of the event loop refers to how the execution context is transferred between the different structures (like the Call Stack and Callback Queue) based on the Event Loop's management.</p>
<h4><strong>Summary:</strong></h4>
<p>The Order of execution as follows</p>
<p><em>Call Stack(functions) --&gt; Job Queue(promises, mutationObserver) ---&gt; Callable Queue(setTimeout(), ....)</em></p>
<p>Reference:
<a href="https://gurindernarang.medium.com/event-looping-in-js-part-1-3cb8fa08121a" title="medium">Event Looping in JS</a></p>]]></description>
<link>https://sraban.github.io/post/event-loop-call-stack-callback-queue-event-table-mapping-microtask</link>
<pubDate>Mon, 24 Jun 2024 22:38:00 +0500</pubDate>
</item>
<item>
<title>Custom Pipe Operator & New Feature, Debugging - RxJS</title>
<description><![CDATA[<p>Creating a custom pipe operator in RxJS allows you to encapsulate complex logic into a reusable operator that you can apply to Observables using the <code>pipe</code> method. This can be particularly useful for operations that you need to perform repeatedly across different parts of your application.</p>
<h4><strong>Key Aspects</strong></h4>
<ul>
<li><strong>Encapsulation:</strong> Encapsulate complex logic into a reusable function.</li>
<li><strong>Reusability:</strong> Use the same operator in different parts of your application.</li>
<li><strong>Readability:</strong> Improve code readability by abstracting complex logic.</li>
</ul>
<p>Here’s a detailed guide on how to create a custom pipe operator in RxJS:</p>
<h2><strong>Steps to Create a Custom Pipe Operator in RxJS</strong></h2>
<h3><strong>1. Import Necessary Functions</strong></h3>
<p>First, import the necessary functions from RxJS. You’ll primarily need <code>Observable</code> and <code>OperatorFunction</code> from <code>rxjs</code>, and <code>map</code> or any other RxJS operators you may want to compose within your custom operator.</p>
<pre><code class="language-javascript">
    import { Observable, OperatorFunction } from 'rxjs';
    import { map } from 'rxjs/operators';
</code></pre>
<h3><strong>2. Define the Operator Function</strong></h3>
<p>Define a function that returns an <code>OperatorFunction</code>. This function should take the parameters you need for your custom logic and return a function that accepts an <code>Observable</code> and returns a new <code>Observable</code>.</p>
<ul>
<li>The <code>OperatorFunction&lt;T, R&gt;</code> type represents a function that takes an <code>Observable&lt;T&gt;</code> and returns an <code>Observable&lt;R&gt;</code>.</li>
</ul>
<pre><code class="language-javascript">
    function myCustomOperator&lt;T, R&gt;(param1: T): OperatorFunction&lt;T, R&gt; {
      return (source: Observable&lt;T&gt;): Observable&lt;R&gt; =&gt; {
        // Implement your custom logic here
        return source.pipe(
          map(value =&gt; {
            // Perform transformation using param1 and value
            const result = /* some transformation logic */;
            return result as unknown as R;
          })
        );
      };
    }
</code></pre>
<h3><strong>3. Implement the Custom Logic</strong></h3>
<p>Inside the function, implement the custom logic. This could involve transforming the data, filtering it, or combining it with other streams. Use the existing RxJS operators to build up your functionality.</p>
<ul>
<li>For example, let’s create a custom operator that multiplies each emitted value by a specified factor.</li>
</ul>
<pre><code class="language-javascript">
    function multiplyBy&lt;T&gt;(factor: number): OperatorFunction&lt;number, number&gt; {
      return (source: Observable&lt;number&gt;): Observable&lt;number&gt; =&gt; {
        return new Observable&lt;number&gt;(subscriber =&gt; {
          const subscription = source.subscribe({
            next(value) {
              subscriber.next(value * factor);
            },
            error(err) {
              subscriber.error(err);
            },
            complete() {
              subscriber.complete();
            }
          });

          // Return the teardown logic
          return () =&gt; {
            subscription.unsubscribe();
          };
        });
      };
    }
</code></pre>
<h3><strong>4. Use the Custom Operator</strong></h3>
<p>Use your custom operator within an Observable pipeline by calling <code>pipe</code> and passing your custom operator.</p>
<ul>
<li>Example usage:</li>
</ul>
<pre><code class="language-javascript">
    import { of } from 'rxjs';

    const source$ = of(1, 2, 3, 4, 5);

    source$
      .pipe(
        multiplyBy(10)  // Use custom operator here
      )
      .subscribe(value =&gt; console.log(value));  // Output: 10, 20, 30, 40, 50
</code></pre>
<h3><strong>5. Handling Errors and Completion</strong></h3>
<p>Ensure that your custom operator properly handles errors and completion. Pass errors and completion notifications from the source Observable to the subscriber.</p>
<ul>
<li>Example with error handling:</li>
</ul>
<pre><code class="language-javascript">
    function safeDivideBy(divisor: number): OperatorFunction&lt;number, number&gt; {
      return (source: Observable&lt;number&gt;): Observable&lt;number&gt; =&gt; {
        return new Observable&lt;number&gt;(subscriber =&gt; {
          const subscription = source.subscribe({
            next(value) {
              if (divisor === 0) {
                subscriber.error(new Error('Division by zero'));
              } else {
                subscriber.next(value / divisor);
              }
            },
            error(err) {
              subscriber.error(err);
            },
            complete() {
              subscriber.complete();
            }
          });

          return () =&gt; {
            subscription.unsubscribe();
          };
        });
      };
    }
</code></pre>
<ul>
<li>Usage:</li>
</ul>
<pre><code class="language-javascript">
    source$
      .pipe(
        safeDivideBy(2)  // Use custom operator
      )
      .subscribe({
        next: value =&gt; console.log(value),
        error: err =&gt; console.error(err.message)  // Output: 0.5, 1, 1.5, 2, 2.5
      });
</code></pre>
<h3><strong>6. Composing with Other Operators</strong></h3>
<p>You can combine your custom operator with other existing RxJS operators to build more complex pipelines.</p>
<ul>
<li>Example with additional operators:</li>
</ul>
<pre><code class="language-javascript">
    source$
      .pipe(
        multiplyBy(2),
        safeDivideBy(4)
      )
      .subscribe(value =&gt; console.log(value));  // Output: 0.5, 1, 1.5, 2, 2.5
</code></pre>
<hr />
<h3><strong>1. Multicasting Operators</strong></h3>
<h4><strong>Overview</strong></h4>
<p>Multicasting allows multiple subscribers to share a single subscription to an Observable, optimizing resource usage. Operators like <code>share</code> and <code>shareReplay</code> are commonly used for this purpose.</p>
<p><strong>Example with <code>shareReplay</code>:</strong></p>
<pre><code class="language-javascript">import { of } from 'rxjs';
import { shareReplay } from 'rxjs/operators';

const source$ = of(1, 2, 3).pipe(shareReplay(1));

source$.subscribe(value =&gt; console.log(`Subscriber 1: ${value}`));
source$.subscribe(value =&gt; console.log(`Subscriber 2: ${value}`));</code></pre>
<h3><strong>2. Higher-Order Observables</strong></h3>
<h4><strong>Overview</strong></h4>
<p>Higher-order Observables are Observables that emit other Observables. Operators like <code>switchMap</code>, <code>concatMap</code>, and <code>mergeMap</code> help manage these higher-order streams.</p>
<p><strong>Example with <code>switchMap</code>:</strong></p>
<pre><code class="language-javascript">import { of, interval } from 'rxjs';
import { switchMap } from 'rxjs/operators';

const source$ = interval(1000).pipe(
  switchMap(() =&gt; of('new observable'))
);

source$.subscribe(value =&gt; console.log(value));</code></pre>
<h3><strong>3. Error Handling Enhancements</strong></h3>
<h4><strong>Overview</strong></h4>
<p>Advanced error handling in RxJS includes operators like <code>catchError</code> and <code>retryWhen</code> to manage errors more effectively.</p>
<p><strong>Example with <code>catchError</code>:</strong></p>
<pre><code class="language-javascript">import { of, throwError } from 'rxjs';
import { catchError } from 'rxjs/operators';

const source$ = throwError('An error occurred').pipe(
  catchError(error =&gt; of(`Caught: ${error}`))
);

source$.subscribe(value =&gt; console.log(value));</code></pre>
<h3><strong>4. Scheduling and Concurrency Control</strong></h3>
<h4><strong>Overview</strong></h4>
<p>Schedulers in RxJS manage the timing of Observable execution. They help control concurrency and the execution context, which is crucial for optimizing performance in complex applications.</p>
<p><strong>Example with <code>asyncScheduler</code>:</strong></p>
<pre><code class="language-javascript">import { of, asyncScheduler } from 'rxjs';
import { observeOn } from 'rxjs/operators';

const source$ = of('Hello World').pipe(
  observeOn(asyncScheduler)
);

source$.subscribe(value =&gt; console.log(value)); // Executes asynchronously</code></pre>
<h3><strong>5. Improved Type Support</strong></h3>
<h4><strong>Overview</strong></h4>
<p>Enhanced type definitions in TypeScript for RxJS operators provide better type safety and auto-completion, making it easier to write and maintain code.</p>
<p><strong>Example with TypeScript:</strong></p>
<pre><code class="language-typescript">
    import { of } from 'rxjs';
    import { map } from 'rxjs/operators';

    const source$ = of&lt;number&gt;(1, 2, 3).pipe(
      map(value =&gt; value * 2)
    );

    source$.subscribe(value =&gt; console.log(value)); // Outputs: 2, 4, 6
</code></pre>
<hr />
<h3><strong>Tooling and Development Tools</strong></h3>
<h4><strong>Overview</strong></h4>
<p>Tools that support RxJS development have evolved, making it easier to develop, debug, and maintain reactive applications. These tools range from debugging utilities to code generators and performance monitors.</p>
<h4><strong>Key Tools</strong></h4>
<ul>
<li><strong>RxJS DevTools:</strong> Provides a way to visualize and debug RxJS streams in a browser.</li>
<li><strong>RxFiddle:</strong> A playground for experimenting with RxJS code and observing its behavior in real-time.</li>
<li><strong>RxJS Marbles:</strong> A tool for testing RxJS observables using marble diagrams.</li>
</ul>
<h4><strong>Detailed Tooling Overview</strong></h4>
<p><strong>RxJS DevTools:</strong></p>
<ul>
<li><strong>Features:</strong> 
<ul>
<li>Visualizes Observable streams.</li>
<li>Debugs subscription chains and events.</li>
<li>Provides insights into stream behaviors and performance.</li>
</ul></li>
<li><strong>Usage:</strong> 
<ul>
<li>Install as a Chrome extension or use a standalone version.</li>
<li>Connect it to your application to start visualizing streams.</li>
</ul></li>
</ul>
<p><strong>RxFiddle:</strong></p>
<ul>
<li><strong>Features:</strong>
<ul>
<li>Interactive RxJS playground.</li>
<li>Supports visualization of operators and data flow.</li>
</ul></li>
<li><strong>Usage:</strong>
<ul>
<li>Write and run RxJS code snippets.</li>
<li>Observe how data flows through operators.</li>
</ul></li>
</ul>
<p><strong>RxJS Marbles:</strong></p>
<ul>
<li><strong>Features:</strong>
<ul>
<li>Enables marble testing for RxJS observables.</li>
<li>Provides a visual representation of stream interactions.</li>
</ul></li>
<li><strong>Usage:</strong>
<ul>
<li>Write tests using marble diagrams to simulate observable emissions.</li>
<li>Verify the expected behavior of streams and operators.</li>
</ul></li>
</ul>]]></description>
<link>https://sraban.github.io/post/custom-pipe-operator-new-feature-debugging-rxjs</link>
<pubDate>Mon, 24 Jun 2024 14:13:00 +0500</pubDate>
</item>
<item>
<title>When and How to use RxJS</title>
<description><![CDATA[<p>RxJS is a powerful tool for handling complex asynchronous operations in a declarative and functional way. While it offers many advantages, such as improved readability, composability, and efficient data handling, it also comes with complexities and challenges, including a steep learning curve and potential performance overhead. Understanding these pros and cons can help you make informed decisions about when and how to use RxJS in your projects.</p>
<h3>1. <strong>Declarative Code</strong></h3>
<ul>
<li><strong>Benefit:</strong> RxJS allows developers to write declarative code for handling asynchronous operations, making it easier to reason about data flows.</li>
<li><strong>Example:</strong> Using <code>map</code>, <code>filter</code>, and <code>reduce</code> to transform and process data streams.</li>
</ul>
<h3>2. <strong>Composability</strong></h3>
<ul>
<li><strong>Benefit:</strong> Operators in RxJS can be easily composed to create complex data pipelines from simpler components.</li>
<li><strong>Example:</strong> Combining multiple data streams using operators like <code>merge</code> and <code>combineLatest</code>.</li>
</ul>
<h3>3. <strong>Powerful Asynchronous Handling</strong></h3>
<ul>
<li><strong>Benefit:</strong> RxJS provides powerful tools for handling complex asynchronous scenarios, such as event handling, timers, and network requests.</li>
<li><strong>Example:</strong> Managing WebSocket connections and handling real-time updates.</li>
</ul>
<h3>4. <strong>Functional Programming Paradigm</strong></h3>
<ul>
<li><strong>Benefit:</strong> Encourages a functional programming approach, leading to more predictable and maintainable code.</li>
<li><strong>Example:</strong> Using pure functions and avoiding side effects with operators like <code>map</code> and <code>filter</code>.</li>
</ul>
<h3>5. <strong>Reactive Programming</strong></h3>
<ul>
<li><strong>Benefit:</strong> Enables reactive programming, where changes in data sources automatically propagate through the system.</li>
<li><strong>Example:</strong> Automatically updating UI components in response to data changes using <code>BehaviorSubject</code>.</li>
</ul>
<h3>6. <strong>Operator Richness</strong></h3>
<ul>
<li><strong>Benefit:</strong> A rich set of operators allows for sophisticated data manipulation and transformation.</li>
<li><strong>Example:</strong> Transforming and filtering data streams with <code>map</code>, <code>filter</code>, and <code>flatMap</code>.</li>
</ul>
<h3>7. <strong>Cancellation and Cleanup</strong></h3>
<ul>
<li><strong>Benefit:</strong> Provides mechanisms for easily canceling and cleaning up subscriptions, preventing resource leaks.</li>
<li><strong>Example:</strong> Using <code>takeUntil</code> or <code>unsubscribe</code> to clean up subscriptions.</li>
</ul>
<h3>8. <strong>Unified Asynchronous API</strong></h3>
<ul>
<li><strong>Benefit:</strong> RxJS unifies handling of various asynchronous patterns (events, promises, etc.) under a single API.</li>
<li><strong>Example:</strong> Converting promises to Observables with <code>fromPromise</code> and handling them consistently.</li>
</ul>
<h3>9. <strong>Real-time Data Handling</strong></h3>
<ul>
<li><strong>Benefit:</strong> Ideal for handling real-time data streams and event-driven applications.</li>
<li><strong>Example:</strong> Using <code>Subject</code> to manage live data feeds, like stock prices or chat messages.</li>
</ul>
<h3>10. <strong>Improved Readability and Maintenance</strong></h3>
<ul>
<li><strong>Benefit:</strong> Declarative and composable code can be easier to read and maintain, especially for complex asynchronous logic.</li>
<li><strong>Example:</strong> Expressing complex event handling logic in a concise and readable way.</li>
</ul>
<hr />
<hr />
<h2><strong>Checklist: Think Before you write RxJS program</strong></h2>
<h3>1. <strong>Steep Learning Curve</strong></h3>
<ul>
<li><strong>Description:</strong> RxJS introduces a new way of thinking about asynchronous programming with concepts like Observables, Operators, and Schedulers.</li>
<li><strong>Challenges:</strong> Developers accustomed to traditional imperative programming may find it difficult to grasp the reactive paradigm and the extensive set of operators.</li>
</ul>
<h3>2. <strong>Large API Surface</strong></h3>
<ul>
<li><strong>Description:</strong> RxJS has a vast number of operators (over 100), each with unique behaviors and use cases.</li>
<li><strong>Challenges:</strong> Learning and remembering the appropriate operator for each task can be daunting, and misuse of operators can lead to performance issues or bugs.</li>
</ul>
<h3>3. <strong>Debugging Difficulty</strong></h3>
<ul>
<li><strong>Description:</strong> Due to the functional and asynchronous nature of RxJS, debugging can be challenging.</li>
<li><strong>Challenges:</strong> Tracing errors and understanding the flow of data through various operators can be complex, especially in large, intricate data streams.</li>
</ul>
<h3>4. <strong>Performance Overhead</strong></h3>
<ul>
<li><strong>Description:</strong> The use of Observables and operators can introduce additional performance overhead compared to direct imperative code.</li>
<li><strong>Challenges:</strong> Inefficient use of operators or excessive use of Observables for simple tasks can lead to performance bottlenecks.</li>
</ul>
<h3>5. <strong>Memory Management</strong></h3>
<ul>
<li><strong>Description:</strong> Managing subscriptions and ensuring that Observables are properly unsubscribed to avoid memory leaks.</li>
<li><strong>Challenges:</strong> Forgetting to unsubscribe from long-lived Observables can cause memory leaks and retain unnecessary resources.</li>
</ul>
<h3>6. <strong>Concurrency Management</strong></h3>
<ul>
<li><strong>Description:</strong> RxJS allows fine-grained control over concurrency and scheduling, which can add complexity.</li>
<li><strong>Challenges:</strong> Understanding and managing concurrency with operators like <code>mergeMap</code>, <code>switchMap</code>, and <code>concatMap</code> requires careful planning.</li>
</ul>
<h3>7. <strong>Integration Complexity</strong></h3>
<ul>
<li><strong>Description:</strong> Integrating RxJS into existing projects and systems can be complex, especially if they are not designed with reactive principles in mind.</li>
<li><strong>Challenges:</strong> Combining reactive code with traditional callback or promise-based code can be tricky and may require additional handling.</li>
</ul>
<h3>8. <strong>Operator Overloading</strong></h3>
<ul>
<li><strong>Description:</strong> The large number of operators and their chaining can lead to over-complication of simple tasks.</li>
<li><strong>Challenges:</strong> Developers might use complex operator chains for tasks that could be handled more simply, making the code harder to read and maintain.</li>
</ul>
<h3>9. <strong>State Management</strong></h3>
<ul>
<li><strong>Description:</strong> Managing state reactively with Observables can be less intuitive compared to traditional state management techniques.</li>
<li><strong>Challenges:</strong> Maintaining and updating application state in a reactive way can require a shift in thinking and careful design.</li>
</ul>
<h3>10. <strong>Type Safety and Compatibility</strong></h3>
<ul>
<li><strong>Description:</strong> Ensuring type safety and compatibility across various Observables and operators, especially in TypeScript.</li>
<li><strong>Challenges:</strong> Misalignments in types can lead to compile-time errors or unexpected runtime behavior.</li>
</ul>
<h3>11. <strong>Overuse of Observables</strong></h3>
<ul>
<li><strong>Drawback:</strong> Over-reliance on Observables can lead to overly complex code for tasks that could be handled more simply.</li>
<li><strong>Impact:</strong> Code can become harder to read, maintain, and debug.</li>
</ul>
<h3>12. <strong>Not Always Intuitive</strong></h3>
<ul>
<li><strong>Drawback:</strong> The reactive paradigm may not be intuitive for all use cases, especially for developers new to functional programming.</li>
<li><strong>Impact:</strong> There can be a mismatch between problem-solving approaches and the reactive model, leading to confusion.</li>
</ul>
<h3>13. <strong>Tooling and Ecosystem Maturity</strong></h3>
<ul>
<li><strong>Drawback:</strong> Tooling for RxJS debugging and profiling is still evolving and may not be as mature as for other paradigms.</li>
<li><strong>Impact:</strong> Limited tooling support can hinder development and troubleshooting.</li>
</ul>
<h3>14. <strong>Potential Overhead in TypeScript</strong></h3>
<ul>
<li><strong>Drawback:</strong> Ensuring type safety with RxJS in TypeScript can add complexity.</li>
<li><strong>Impact:</strong> Complex type definitions and generics can make the codebase harder to understand and maintain.</li>
</ul>]]></description>
<link>https://sraban.github.io/post/when-and-how-to-use-rxjs</link>
<pubDate>Mon, 24 Jun 2024 14:01:00 +0500</pubDate>
</item>
<item>
<title>Angular Performance Optimisation</title>
<description><![CDATA[<p>List of performance optimization steps for Angular applications</p>
<h3>1. <strong>Event Coalescing</strong>: <code>{ngZoneEventCoalescing: true}</code></h3>
<ul>
<li><strong>Description</strong>: Aggregates multiple events into a single change detection run.</li>
<li><strong>Benefits</strong>: Reduces the number of change detection cycles.</li>
</ul>
<h3>2. <strong>Zone.js Optimization</strong>: <code>{ngZone: 'noop'}</code></h3>
<ul>
<li><strong>Description</strong>: Disables Zone.js to manage change detection manually.</li>
<li><strong>Benefits</strong>: Provides fine-grained control over change detection, reducing unnecessary cycles.</li>
</ul>
<h3>3. <strong>OnPush Change Detection</strong></h3>
<ul>
<li><strong>Description</strong>: Changes the default change detection strategy to <code>OnPush</code>, which only checks for changes when input properties change.</li>
<li>
<p><strong>Benefits</strong>: Limits change detection to specific component updates, improving performance.</p>
<pre><code class="language-typescript">@Component({
  selector: 'app-my-component',
  templateUrl: './my-component.component.html',
  changeDetection: ChangeDetectionStrategy.OnPush
})</code></pre>
</li>
</ul>
<h3>4. <strong>Track By Function in ngFor</strong></h3>
<ul>
<li><strong>Description</strong>: Uses a <code>trackBy</code> function in <code>ngFor</code> to identify items in a collection by a unique identifier.</li>
<li>
<p><strong>Benefits</strong>: Minimizes DOM re-renders by tracking changes more efficiently.</p>
<pre><code class="language-html">&lt;div *ngFor="let item of items; trackBy: trackByFn"&gt;
  {{item.name}}
&lt;/div&gt;</code></pre>
<pre><code class="language-typescript">trackByFn(index: number, item: any): number {
  return item.id; // unique id corresponding to item
}</code></pre>
</li>
</ul>
<h3>5. <strong>Lazy Loading Modules</strong></h3>
<ul>
<li><strong>Description</strong>: Defers loading of modules until they are needed, rather than loading all modules at startup.</li>
<li>
<p><strong>Benefits</strong>: Reduces initial load time and improves application performance.</p>
<pre><code class="language-typescript">const routes: Routes = [
  {
      path: 'feature',
      loadChildren: () =&gt; import('./feature/feature.module').then(m =&gt; m.FeatureModule)
  }
];</code></pre>
</li>
</ul>
<h3>6. <strong>Preload Lazy-Loaded Modules</strong></h3>
<ul>
<li><strong>Description</strong>: Uses Angular’s <code>PreloadAllModules</code> strategy to preload lazy-loaded modules after the initial load.</li>
<li>
<p><strong>Benefits</strong>: Balances initial load time with module availability.</p>
<pre><code class="language-typescript">RouterModule.forRoot(routes, { preloadingStrategy: PreloadAllModules })</code></pre>
</li>
</ul>
<h3>7. <strong>AOT Compilation (Ahead of Time)</strong></h3>
<ul>
<li><strong>Description</strong>: Pre-compiles the application at build time instead of runtime.</li>
<li>
<p><strong>Benefits</strong>: Reduces application size and improves startup performance.</p>
<pre><code class="language-json">"angularCompilerOptions": {
  "enableIvy": true,
  "aot": true
}</code></pre>
</li>
</ul>
<h3>8. <strong>Tree Shaking</strong></h3>
<ul>
<li><strong>Description</strong>: Removes unused code during the build process.</li>
<li>
<p><strong>Benefits</strong>: Decreases the size of the final JavaScript bundle, leading to faster load times.</p>
<pre><code class="language-json">"optimization": {
  "minify": true,
  "removeUnusedDependencies": true
}</code></pre>
</li>
</ul>
<h3>9. <strong>Optimize Image Loading</strong></h3>
<ul>
<li><strong>Description</strong>: Uses modern image formats (like WebP), lazy loading, and responsive images.</li>
<li>
<p><strong>Benefits</strong>: Reduces the load on the network and improves rendering performance.</p>
<pre><code class="language-html">&lt;img src="image.webp" loading="lazy" alt="description"&gt;</code></pre>
</li>
</ul>
<h3>10. <strong>Code Splitting</strong></h3>
<ul>
<li><strong>Description</strong>: Splits the application code into smaller chunks that are loaded on demand.</li>
<li>
<p><strong>Benefits</strong>: Improves load times by only loading the necessary code for each route.</p>
<pre><code class="language-json">"optimization": {
  "splitChunks": {
      "chunks": "all"
  }
}</code></pre>
</li>
</ul>
<h3>11. <strong>Use Web Workers</strong></h3>
<ul>
<li><strong>Description</strong>: Offloads heavy computation to background threads.</li>
<li>
<p><strong>Benefits</strong>: Prevents blocking the main thread, maintaining a responsive UI.</p>
<pre><code class="language-typescript">if (typeof Worker !== 'undefined') {
  const worker = new Worker(new URL('./app.worker', import.meta.url));
  worker.onmessage = ({ data }) =&gt; {
      console.log(`Page received message: ${data}`);
  };
  worker.postMessage('hello');
}</code></pre>
</li>
</ul>
<h3>12. <strong>Service Worker for Caching</strong></h3>
<ul>
<li><strong>Description</strong>: Uses Angular’s service worker to cache application resources for offline access and faster load times.</li>
<li>
<p><strong>Benefits</strong>: Reduces network requests and improves load performance.</p>
<pre><code class="language-typescript">import { ServiceWorkerModule } from '@angular/service-worker';
import { environment } from '../environments/environment';

ServiceWorkerModule.register('ngsw-worker.js', {
  enabled: environment.production,
  registrationStrategy: 'registerWhenStable:30000'
})</code></pre>
</li>
</ul>
<h3>13. <strong>Use <code>ng-container</code> to Group Elements</strong></h3>
<ul>
<li><strong>Description</strong>: Uses <code>ng-container</code> to avoid unnecessary <code>&lt;div&gt;</code> tags and reduce the size of the DOM tree.</li>
<li>
<p><strong>Benefits</strong>: Minimizes the number of elements in the DOM, leading to improved rendering performance.</p>
<pre><code class="language-html">&lt;ng-container *ngIf="condition"&gt;
  &lt;p&gt;Content&lt;/p&gt;
&lt;/ng-container&gt;</code></pre>
</li>
</ul>
<h3>14. <strong>Throttle or Debounce Event Handlers</strong></h3>
<ul>
<li><strong>Description</strong>: Uses RxJS operators to limit the frequency of event handling.</li>
<li>
<p><strong>Benefits</strong>: Reduces the number of function executions and subsequent change detection runs.</p>
<pre><code class="language-typescript">import { fromEvent } from 'rxjs';
import { debounceTime } from 'rxjs/operators';

fromEvent(document, 'click')
  .pipe(debounceTime(300))
  .subscribe((event) =&gt; console.log(event));</code></pre>
</li>
</ul>
<h3>15. <strong>Memory Leak Prevention</strong></h3>
<ul>
<li><strong>Description</strong>: Ensures subscriptions are properly unsubscribed and components are destroyed cleanly.</li>
<li>
<p><strong>Benefits</strong>: Prevents memory leaks, which can degrade performance over time.</p>
<pre><code class="language-typescript">import { Subscription } from 'rxjs';
let subscription: Subscription = new Subscription();

ngOnDestroy() {
  subscription.unsubscribe();
}</code></pre>
</li>
</ul>
<h3>16. <strong>Avoid Direct DOM Manipulation</strong></h3>
<ul>
<li><strong>Description</strong>: Uses Angular’s templating system instead of direct DOM manipulation.</li>
<li>
<p><strong>Benefits</strong>: Ensures changes are tracked by Angular's change detection, maintaining application consistency.</p>
<pre><code class="language-typescript">this.renderer.setStyle(element, 'display', 'none');</code></pre>
</li>
</ul>
<h3>17. <strong>Defer and Async Loading for Scripts</strong></h3>
<ul>
<li><strong>Description</strong>: Loads external scripts with <code>defer</code> or <code>async</code> attributes.</li>
<li>
<p><strong>Benefits</strong>: Prevents blocking the main thread during script loading, improving initial render times.</p>
<pre><code class="language-html">&lt;script src="example.js" defer&gt;&lt;/script&gt;</code></pre>
</li>
</ul>
<h3>18. <strong>Minimize Use of <code>*ngIf</code> and <code>*ngFor</code></strong></h3>
<ul>
<li><strong>Description</strong>: Reduces the use of <code>*ngIf</code> and <code>*ngFor</code> directives to avoid frequent DOM changes.</li>
<li>
<p><strong>Benefits</strong>: Decreases change detection and re-rendering overhead.</p>
<pre><code class="language-html">&lt;ng-container *ngIf="condition"&gt;
  &lt;!-- Conditional content here --&gt;
&lt;/ng-container&gt;</code></pre>
</li>
</ul>
<h3>19. <strong>Enable Production Mode</strong></h3>
<ul>
<li><strong>Description</strong>: Ensures Angular runs in production mode, which disables development mode checks and optimizations.</li>
<li>
<p><strong>Benefits</strong>: Reduces overhead and improves application performance.</p>
<pre><code class="language-typescript">import { enableProdMode } from '@angular/core';
import { environment } from './environments/environment';

if (environment.production) {
  enableProdMode();
}</code></pre>
</li>
</ul>
<h3>Configure tree shaking in angular app</h3>
<p>Tree shaking is an optimization technique used to eliminate dead code (unused code) from a JavaScript bundle. In Angular applications, tree shaking is handled primarily by the build tool, which is usually Webpack, as part of the Angular CLI's build process. Here’s how you can ensure tree shaking is effectively implemented in your Angular application:</p>
<h3><strong>Key Steps to Ensure Tree Shaking in Angular Applications</strong></h3>
<ol>
<li>
<p><strong>Ensure Production Mode is Enabled</strong></p>
<p>Angular's build process differentiates between development and production builds. Production builds enable optimizations like tree shaking, minification, and Ahead-of-Time (AOT) compilation.</p>
<p><strong>How to Enable:</strong></p>
<ul>
<li>
<p><strong>Via Command Line</strong>:
Run your build command with the <code>--prod</code> flag:</p>
<pre><code class="language-bash">ng build --prod</code></pre>
</li>
<li>
<p><strong>In <code>angular.json</code> Configuration</strong>:
Ensure your <code>production</code> configuration has <code>optimization</code> enabled.</p>
<pre><code class="language-json">"configurations": {
 "production": {
     "optimization": true,
     "outputHashing": "all",
     "sourceMap": false,
     "extractCss": true,
     "namedChunks": false,
     "aot": true,
     "extractLicenses": true,
     "vendorChunk": false,
     "buildOptimizer": true
 }
}</code></pre>
</li>
</ul>
</li>
<li>
<p><strong>Use ES6 Module Imports</strong></p>
<p>Tree shaking works best with ES6 module syntax because the static structure of ES6 imports and exports makes it easier for the build tool to determine which code is used and which isn’t.</p>
<p><strong>Example:</strong></p>
<pre><code class="language-typescript"> // Import only the required parts
 import { Component, OnInit } from '@angular/core';</code></pre>
</li>
<li>
<p><strong>Avoid Dynamic <code>require</code> Statements</strong></p>
<p>Avoid using dynamic <code>require</code> statements, as they can prevent tree shaking. Always use static imports.</p>
<p><strong>Good:</strong></p>
<pre><code class="language-typescript">import { MyModule } from './path/to/module';</code></pre>
<p><strong>Bad:</strong></p>
<pre><code class="language-typescript">// Avoid dynamic imports that bring in unnecessary code
const MyModule = require('./path/to/module');
const Component = require('@angular/core').Component;

// Sometime
  import('large-module').then(module =&gt; {
    module.someFunction();
  });</code></pre>
</li>
<li>
<p><strong>Optimize Angular CLI Configuration (<code>angular.json</code>)</strong></p>
<p>Ensure your <code>angular.json</code> file is set up to take advantage of the Angular CLI's optimizations.</p>
<p><strong>Configuration Snippet:</strong></p>
<pre><code class="language-json">"optimization": {
   "scripts": true,
   "styles": true,
   "fonts": true
},
"buildOptimizer": true,</code></pre>
</li>
<li>
<p><strong>Minimize Polyfills</strong></p>
<p>Polyfills can add significant bloat to your application if not carefully managed. Only include necessary polyfills.</p>
<p><strong>Example:</strong></p>
<ul>
<li>Remove unnecessary polyfills from <code>polyfills.ts</code>.</li>
</ul>
</li>
<li>
<p><strong>Use <code>sideEffects</code> in <code>package.json</code></strong></p>
<p>Specify that your code is side-effect free to help Webpack in identifying and eliminating dead code. The <code>sideEffects</code> field in <code>package.json</code> indicates which files are safe to prune.</p>
<p><strong>Example:</strong></p>
<pre><code class="language-json">{
   "name": "your-app",
   "version": "1.0.0",
   "sideEffects": false
}</code></pre>
</li>
<li>
<p><strong>Use Angular CLI's Tree Shakable Libraries</strong></p>
<p>Angular CLI supports tree-shakable libraries. Ensure the libraries you use are designed to support tree shaking by checking their <code>package.json</code> for proper module fields like <code>es2015</code>, <code>module</code>, or <code>esnext</code>.</p>
</li>
<li>
<p><strong>Optimize Third-Party Library Imports</strong></p>
<p>Import only what you need from third-party libraries instead of the entire library.</p>
<p><strong>Example:</strong></p>
<pre><code class="language-typescript">// Good: Import only required parts
import { debounceTime } from 'rxjs/operators';

// Bad: Import the whole library
import * as _ from 'lodash';</code></pre>
</li>
<li>
<p><strong>Use AOT Compilation</strong></p>
<p>AOT compilation precompiles the Angular application before the browser downloads and runs it. This helps reduce the bundle size and improve the runtime performance, indirectly aiding tree shaking.</p>
<p><strong>How to Enable:</strong></p>
<ul>
<li>
<p><strong>Via Command Line</strong>:</p>
<pre><code class="language-bash">ng build --aot</code></pre>
</li>
<li><strong>In <code>angular.json</code></strong>:
<pre><code class="language-json">"aot": true</code></pre></li>
</ul>
</li>
<li>
<p><strong>Review Webpack Configuration (Advanced)</strong></p>
<p>Although Angular CLI abstracts Webpack configuration, advanced users might want to review and adjust Webpack settings directly to ensure tree shaking is optimized.</p>
<p><strong>Example:</strong></p>
<ul>
<li>
<p>Custom Webpack configuration through Angular CLI builders like <code>@angular-builders/custom-webpack</code>.</p>
<pre><code class="language-json">"architect": {
  "build": {
      "builder": "@angular-builders/custom-webpack:browser",
      "options": {
          "customWebpackConfig": {
              "path": "./extra-webpack.config.js"
          },
          // other options...
      }
  }
}</code></pre>
</li>
</ul>
</li>
<li>
<p><strong>Monitor and Analyze Bundle Size</strong></p>
<p>Regularly analyze your bundle size to ensure tree shaking is effectively reducing the application size. Tools like <code>source-map-explorer</code> or <code>webpack-bundle-analyzer</code> can be used for this purpose.</p>
<p><strong>Example:</strong></p>
<pre><code class="language-bash">  npm install -g source-map-explorer
  source-map-explorer dist/your-app/main.*.js</code></pre>
</li>
</ol>
<h3>Summary</h3>
<p>To ensure effective tree shaking in your Angular application:</p>
<ol>
<li><strong>Enable Production Mode</strong> in your build configuration.</li>
<li><strong>Use ES6 Module Imports</strong> and avoid dynamic requires.</li>
<li><strong>Optimize Angular CLI Configuration</strong> for tree shaking.</li>
<li><strong>Minimize Polyfills</strong> and specify side effects in <code>package.json</code>.</li>
<li><strong>Leverage Tree Shakable Libraries</strong> and optimize third-party imports.</li>
<li><strong>Utilize AOT Compilation</strong> for pre-compilation benefits.</li>
<li><strong>Analyze Bundle Size</strong> to verify the effectiveness of tree shaking.</li>
</ol>
<p>By following these steps, you can significantly reduce your application's bundle size, leading to faster load times and better performance.</p>
<h3>Conclusion</h3>
<p>Applying these performance optimization steps can significantly enhance the responsiveness, efficiency, and overall user experience of an Angular application. Each step targets specific aspects of application performance, from initial load times to runtime efficiency and resource management.</p>]]></description>
<link>https://sraban.github.io/post/angular-performance-optimisation</link>
<pubDate>Mon, 24 Jun 2024 11:07:00 +0500</pubDate>
</item>
<item>
<title>ES6 Features</title>
<description><![CDATA[<p>Below is a comprehensive list of these features:
ES6 = ECMAScript 2015</p>
<h3>1. <strong>Arrow Functions</strong></h3>
<ul>
<li>Syntactic sugar for anonymous functions with a concise syntax.</li>
<li>
<p>Does not have its own <code>this</code> context, which is lexically scoped.</p>
<pre><code class="language-javascript">const add = (a, b) =&gt; a + b;</code></pre>
<h3>2. <strong>Block-Scoped Variables: <code>let</code> and <code>const</code></strong></h3>
</li>
<li><code>let</code>: Allows block-scoped variables.</li>
<li>
<p><code>const</code>: Declares block-scoped constants that cannot be reassigned.</p>
<pre><code class="language-javascript">  let variable = "mutable";
  const constant = "immutable";</code></pre>
<h3>3. <strong>Default Parameters</strong></h3>
</li>
<li>
<p>Allows function parameters to have default values.</p>
<pre><code class="language-javascript">function greet(name = "World") {
    return `Hello, ${name}`;
}</code></pre>
</li>
</ul>
<h3>4. <strong>Template Literals</strong></h3>
<ul>
<li>
<p>Multi-line strings and string interpolation using backticks (`).</p>
<pre><code class="language-javascript">const name = "John";
const greeting = `Hello, ${name}!`;</code></pre>
</li>
</ul>
<h3>5. <strong>Destructuring Assignment</strong></h3>
<ul>
<li>
<p>Extract values from arrays or properties from objects into distinct variables.</p>
<pre><code class="language-javascript">const [x, y] = [1, 2];
const { name, age } = { name: "Alice", age: 25 };</code></pre>
</li>
</ul>
<h3>6. <strong>Rest and Spread Operators</strong></h3>
<ul>
<li><code>Rest</code>: Collects arguments into an array.</li>
<li>
<p><code>Spread</code>: Expands elements of an array or object.</p>
<pre><code class="language-javascript">const sum = (...args) =&gt; args.reduce((a, b) =&gt; a + b, 0); // Rest
const arr = [1, 2, 3];
const newArr = [...arr, 4, 5]; // Spread</code></pre>
</li>
</ul>
<h3>7. <strong>Enhanced Object Literals</strong></h3>
<ul>
<li>
<p>Shorthand property names, method definitions, and computed property names.</p>
<pre><code class="language-javascript">const name = "Alice";
const person = {
  name,
  greet() {
    console.log(`Hello, ${this.name}`);
  },
};</code></pre>
</li>
</ul>
<h3>8. <strong>Classes</strong></h3>
<ul>
<li>
<p>Syntactic sugar for creating objects and handling inheritance.</p>
<pre><code class="language-javascript">class Person {
  constructor(name) {
    this.name = name;
  }
  greet() {
    console.log(`Hello, ${this.name}`);
  }
}</code></pre>
</li>
</ul>
<h3>9. <strong>Modules (import/export)</strong></h3>
<ul>
<li>
<p>Official support for module loading with <code>import</code> and <code>export</code> keywords.</p>
<pre><code class="language-javascript">// module.js
export const name = "Alice";

// main.js
import { name } from './module.js';</code></pre>
</li>
</ul>
<h3>10. <strong>Promises</strong></h3>
<ul>
<li>
<p>Asynchronous operations management with <code>then</code>, <code>catch</code>, and <code>finally</code>.</p>
<pre><code class="language-javascript">const promise = new Promise((resolve, reject) =&gt; {
  setTimeout(() =&gt; resolve("Done"), 1000);
});

promise.then(result =&gt; console.log(result));</code></pre>
</li>
</ul>
<h3>11. <strong>Block-Scoped Functions</strong></h3>
<ul>
<li>
<p>Functions defined in a block are scoped to that block.</p>
<pre><code class="language-javascript">if (true) {
  function sayHi() {
    console.log("Hi");
  }
  sayHi();
}</code></pre>
</li>
</ul>
<h3>12. <strong>Generators</strong></h3>
<ul>
<li>
<p>Functions that can be paused and resumed, maintaining their own state.</p>
<pre><code class="language-javascript">function* generator() {
yield 1;
yield 2;
return 3;
}</code></pre>
</li>
</ul>
<h3>13. <strong>New Built-in Methods</strong></h3>
<ul>
<li>
<p>New methods for various objects like <code>Array</code>, <code>String</code>, <code>Object</code>, etc.</p>
<pre><code class="language-javascript">const str = "hello";
str.includes("e"); // true

const arr = Array.from("hello");</code></pre>
</li>
</ul>
<h3>14. <strong>New Collections: Map, Set, WeakMap, WeakSet</strong></h3>
<ul>
<li><code>Map</code>: Key-value pairs.</li>
<li><code>Set</code>: Unique values.</li>
<li>
<p><code>WeakMap</code> and <code>WeakSet</code>: Garbage-collection friendly collections.</p>
<pre><code class="language-javascript">const map = new Map();
map.set('key', 'value');

const set = new Set([1, 2, 2, 3]);</code></pre>
</li>
</ul>
<h3>15. <strong>for...of Loop</strong></h3>
<ul>
<li>
<p>Iterates over iterable objects like arrays, strings, etc.</p>
<pre><code class="language-javascript">for (const item of [1, 2, 3]) {
  console.log(item);
}</code></pre>
</li>
</ul>
<h3>16. <strong>Binary and Octal Literals</strong></h3>
<ul>
<li>
<p>Literal notations for binary (<code>0b</code>) and octal (<code>0o</code>) numbers.</p>
<pre><code class="language-javascript">const binary = 0b1010;
const octal = 0o755;</code></pre>
</li>
</ul>
<h3>17. <strong>New Target Meta-Property</strong></h3>
<ul>
<li>
<p>Provides a reference to the target of a function call.</p>
<pre><code class="language-javascript">class Parent {
  constructor() {
    console.log(new.target.name);
  }
}

class Child extends Parent {
  constructor() {
    super();
  }
}

new Child(); // logs "Child"</code></pre>
</li>
</ul>
<h3>18. <strong>New Math, Number, String, Object Methods</strong></h3>
<ul>
<li>
<p>New methods for mathematical computations, number checking, string manipulation, and object handling.</p>
<pre><code class="language-javascript">Number.isFinite(42); // true
Math.trunc(4.9); // 4
Object.assign({}, {a: 1}, {b: 2}); // {a: 1, b: 2}</code></pre>
</li>
</ul>
<h3>19. <strong>Reflect API</strong></h3>
<ul>
<li>
<p>Provides methods for interceptable JavaScript operations, similar to the proxy handlers.</p>
<pre><code class="language-javascript">const obj = { x: 1 };
Reflect.set(obj, 'x', 2);</code></pre>
</li>
</ul>
<h3>20. <strong>Proxy API</strong></h3>
<ul>
<li>
<p>Used to define custom behavior for fundamental operations (e.g., property lookup, assignment).</p>
<pre><code class="language-javascript">const handler = {
  get: (target, property) =&gt; {
    return property in target ? target[property] : 42;
  }
};

const proxy = new Proxy({}, handler);
console.log(proxy.foo); // 42</code></pre>
</li>
</ul>
<h3>21. <strong>Symbol Type</strong></h3>
<ul>
<li>
<p>A new primitive type for creating unique identifiers.</p>
<pre><code class="language-javascript">const sym = Symbol("identifier");
const obj = { [sym]: "value" };</code></pre>
</li>
</ul>
<hr />
<h4>In Summary:</h4>
<h5>Arrow functions, let and const, default parameters, template       literals, destructuring assignment, rest and spread operators,       enhanced object literals, classes, modules (import/export), promises,    block-scoped functions, generators, new built-in methods       (String.prototype.includes, Array.from, etc.), new collections (Map,     Set, WeakMap, WeakSet), for...of loop, binary and octal literals, new    target meta-property, new Math, Number, String, Object methods,       Reflect API, Proxy API, Symbol type.</h5>
<hr />
<ul>
<li><strong>declaration</strong> - let,const</li>
<li><strong>data type(primitive, nonprimitive)</strong> - Symbol, binary, octal</li>
<li><strong>operator</strong> -&gt; ??, ...</li>
<li><strong>enhanced object literal , template literal</strong></li>
<li><strong>conditions/loop</strong> -&gt; (for of loop)</li>
<li><strong>function</strong> - blocked scope, default params assignment</li>
<li><strong>generators</strong> yeild</li>
<li><strong>class</strong> - new.target.name</li>
<li><strong>new methods</strong> - String, Number, Array, Object Prototype</li>
<li><strong>new collection</strong> - Map, Set, WeekMap, WeekSet</li>
<li><strong>module</strong> - export/import</li>
<li><strong>API</strong> - Math, Proxy, Reflect</li>
</ul>
<hr />
<p><img src="https://i.imgur.com/NaX1CXC.png" alt="medium" /></p>]]></description>
<link>https://sraban.github.io/post/es6-features</link>
<pubDate>Mon, 24 Jun 2024 07:24:00 +0500</pubDate>
</item>
<item>
<title>tsConfig.json configuration details</title>
<description><![CDATA[<p>The <code>tsconfig.json</code> file is essential for configuring TypeScript projects, providing a wide range of options to control the compilation process. Below is a comprehensive list of <code>tsconfig.json</code> properties along with detailed descriptions and examples for each.</p>
<h3>Overall Structure</h3>
<p>A typical <code>tsconfig.json</code> file looks like this:</p>
<pre><code>{
  "compilerOptions": {
    // Compiler options go here
  },
  "include": [
    // File patterns to include
  ],
  "exclude": [
    // File patterns to exclude
  ],
  "files": [
    // Specific files to include
  ],
  "references": [
    // References to other tsconfig files for project references
  ],
  "extends": "path/to/base/tsconfig",
  "typeAcquisition": {
    // Type acquisition options for type definitions
  },
  "compileOnSave": true,
  "tsBuildInfoFile": "path/to/tsbuildinfo",
  "watchOptions": {
    // Options for the watch mode
  },
  "assumeChangesOnlyAffectDirectDependencies": true,
  "plugins": [
    // Plugins to extend TypeScript functionality
  ]
}</code></pre>
<h3>Detailed Descriptions</h3>
<h4><code>compilerOptions</code></h4>
<ul>
<li><strong>Purpose</strong>: Contains settings that influence the behavior of the TypeScript compiler.</li>
<li><strong>Type</strong>: <code>object</code></li>
<li>
<p><strong>Common Properties</strong>:</p>
<ul>
<li>
<p><strong><code>target</code></strong>: Specifies the output JavaScript version. Common values are <code>"ES5"</code>, <code>"ES6"</code>, <code>"ES2015"</code>, <code>"ES2020"</code>, etc.
<code>{target": "ES6"}</code></p>
</li>
<li>
<p><strong><code>module</code></strong>: Determines the module code generation. Options include <code>"commonjs"</code>, <code>"amd"</code>, <code>"esnext"</code>, etc.
<code>{"module": "commonjs"}</code></p>
</li>
<li>
<p><strong><code>strict</code></strong>: Enables all strict type-checking options.
<code>{"strict": true}</code></p>
</li>
<li>
<p><strong><code>outDir</code></strong>: Specifies the output directory for compiled files.
<code>{ "outDir": "./dist"}</code></p>
</li>
<li>
<p><strong><code>rootDir</code></strong>: Specifies the root directory of input files.
<code>{"rootDir": "./src"}</code></p>
</li>
<li>
<p><strong><code>sourceMap</code></strong>: Generates corresponding <code>.map</code> files for debugging.
<code>{ "sourceMap": true}</code></p>
</li>
<li>
<p><strong><code>declaration</code></strong>: Generates corresponding <code>.d.ts</code> files for TypeScript definitions.
<code>{"declaration": true}</code></p>
</li>
<li>
<p><strong><code>moduleResolution</code></strong>: Specifies the module resolution strategy, such as <code>"node"</code> or <code>"classic"</code>.
<code>{"moduleResolution": "node"}</code></p>
</li>
<li>
<p><strong><code>lib</code></strong>: Includes standard library files in the compilation.
<code>{"lib": ["ES6", "DOM"]}</code></p>
</li>
<li>
<p><strong><code>esModuleInterop</code></strong>: Enables interoperability between CommonJS and ES modules.
<code>{"esModuleInterop": true}</code></p>
</li>
<li>
<p><strong><code>jsx</code></strong>: Specifies the JSX code generation for React. Options are <code>"preserve"</code>, <code>"react"</code>, <code>"react-jsx"</code>, etc.
<code>{"jsx": "react"}</code></p>
</li>
<li>
<p><strong><code>allowJs</code></strong>: Allows JavaScript files to be compiled.
<code>{"allowJs": true}</code></p>
</li>
<li>
<p><strong><code>checkJs</code></strong>: Type-checks JavaScript files.
<code>{"checkJs": true}</code></p>
</li>
<li>
<p><strong><code>baseUrl</code></strong>: Base directory for resolving non-relative module names.
<code>{"baseUrl": "./"}</code></p>
</li>
<li>
<p><strong><code>paths</code></strong>: A set of path mappings for module resolution.
<code>{"paths": { "@app/*": ["src/app/*"] }}</code></p>
</li>
<li>
<p><strong><code>typeRoots</code></strong>: Specifies directories containing type definitions.
<code>{"typeRoots": ["./node_modules/@types"]}</code></p>
</li>
<li>
<p><strong><code>noEmit</code></strong>: Disables the emitting of output files.
<code>{"noEmit": true}</code></p>
</li>
<li>
<p><strong><code>skipLibCheck</code></strong>: Skips type checking of declaration files.
<code>{"skipLibCheck": true}</code></p>
</li>
<li>
<p><strong><code>noImplicitAny</code></strong>: Raises errors on expressions and declarations with an implied <code>any</code> type.
<code>{"noImplicitAny": true}</code></p>
</li>
<li>
<p><strong><code>resolveJsonModule</code></strong>: Enables importing JSON modules.
<code>{"resolveJsonModule": true}</code></p>
</li>
<li>
<p><strong><code>emitDecoratorMetadata</code></strong>: Emits design-type metadata for decorators.
<code>{"emitDecoratorMetadata": true}</code></p>
</li>
<li>
<p><strong><code>experimentalDecorators</code></strong>: Enables experimental support for decorators.
<code>{"experimentalDecorators": true}</code></p>
</li>
<li>
<p><strong><code>incremental</code></strong>: Enables incremental compilation, which speeds up builds.
<code>{ "incremental": true}</code></p>
</li>
<li>
<p><strong><code>composite</code></strong>: Enables project compilation.
<code>{"composite": true}</code></p>
</li>
<li><strong><code>tsBuildInfoFile</code></strong>: Specifies the location of the build information file.
<code>{"tsBuildInfoFile": "./build/cache/tsbuildinfo.json"}</code></li>
</ul>
</li>
</ul>
<h4><code>include</code></h4>
<ul>
<li><strong>Purpose</strong>: Specifies an array of file patterns to include in the compilation.</li>
<li><strong>Type</strong>: <code>array</code></li>
<li><strong>Example</strong>:
<code>{"include": ["src/**/*", "tests/**/*"]}</code></li>
</ul>
<h4><code>exclude</code></h4>
<ul>
<li><strong>Purpose</strong>: Specifies an array of file patterns to exclude from the compilation.</li>
<li><strong>Type</strong>: <code>array</code></li>
<li><strong>Example</strong>:
<code>{"exclude": ["node_modules", "dist", "build"]}</code></li>
</ul>
<h4><code>files</code></h4>
<ul>
<li><strong>Purpose</strong>: Lists specific files to include in the compilation, regardless of the <code>include</code> and <code>exclude</code> settings.</li>
<li><strong>Type</strong>: <code>array</code></li>
<li><strong>Example</strong>:
<code>{"files": ["src/index.ts", "src/globals.d.ts"]}</code></li>
</ul>
<h4><code>references</code></h4>
<ul>
<li><strong>Purpose</strong>: Specifies a list of project references, allowing TypeScript projects to depend on other TypeScript projects.</li>
<li><strong>Type</strong>: <code>array</code></li>
<li><strong>Example</strong>:
<code>{ "references": [ { "path": "../common" }, { "path": "../utilities" } ]}</code></li>
</ul>
<h4><code>extends</code></h4>
<ul>
<li><strong>Purpose</strong>: Allows the <code>tsconfig.json</code> to extend another configuration file.</li>
<li><strong>Type</strong>: <code>string</code></li>
<li><strong>Example</strong>:
<code>{ "extends": "./base/tsconfig.json" }</code></li>
</ul>
<h4><code>typeAcquisition</code></h4>
<ul>
<li><strong>Purpose</strong>: Controls the acquisition of type definitions for JavaScript projects.</li>
<li><strong>Type</strong>: <code>object</code></li>
<li><strong>Properties</strong>:
<ul>
<li><strong><code>enable</code></strong>: Enables type acquisition.
<code>{ "enable": true }</code></li>
<li><strong><code>include</code></strong>: Specifies a list of type definitions to include.
<code>{ "include": ["jest", "lodash"] }</code></li>
<li><strong><code>exclude</code></strong>: Specifies a list of type definitions to exclude.
<code>{ "exclude": ["express"] }</code></li>
</ul></li>
</ul>
<h4><code>compileOnSave</code></h4>
<ul>
<li><strong>Purpose</strong>: If set to <code>true</code>, TypeScript will compile files upon saving in the IDE.</li>
<li><strong>Type</strong>: <code>boolean</code></li>
<li><strong>Example</strong>:
<code>{ "compileOnSave": true }</code></li>
</ul>
<h4><code>tsBuildInfoFile</code></h4>
<ul>
<li><strong>Purpose</strong>: Specifies the location of the build information file to store incremental compilation information.</li>
<li><strong>Type</strong>: <code>string</code></li>
<li><strong>Example</strong>:
<code>{ "tsBuildInfoFile": "./build/cache/tsbuildinfo.json" }</code></li>
</ul>
<h4><code>watchOptions</code></h4>
<ul>
<li><strong>Purpose</strong>: Specifies options for watching files in watch mode.</li>
<li><strong>Type</strong>: <code>object</code></li>
<li><strong>Properties</strong>:
<ul>
<li><strong><code>watchFile</code></strong>: Strategy for watching files.
<code>{ "watchFile": "useFsEvents" }</code></li>
<li><strong><code>watchDirectory</code></strong>: Strategy for watching directories.
<code>{ "watchDirectory": "useFsEvents" }</code></li>
<li><strong><code>fallbackPolling</code></strong>: Polling strategy for systems where <code>fs.watch</code> cannot be used.
<code>{ "fallbackPolling": "dynamicPriority" }</code></li>
</ul></li>
</ul>
<h4><code>assumeChangesOnlyAffectDirectDependencies</code></h4>
<ul>
<li><strong>Purpose</strong>: When set to <code>true</code>, TypeScript assumes changes only affect direct dependencies, which can speed up incremental builds.</li>
<li><strong>Type</strong>: <code>boolean</code></li>
<li><strong>Example</strong>:
<code>{ "assumeChangesOnlyAffectDirectDependencies": true }</code></li>
</ul>
<h4><code>plugins</code></h4>
<ul>
<li><strong>Purpose</strong>: Specifies a list of plugins that extend TypeScript's functionality.</li>
<li><strong>Type</strong>: <code>array</code></li>
<li><strong>Example</strong>:
<code>{ "plugins": [ { "name": "typescript-tslint-plugin" }, { "name": "typescript-styled-plugin" } ] }</code></li>
</ul>]]></description>
<link>https://sraban.github.io/post/tsconfigjson-configuration-details</link>
<pubDate>Sun, 23 Jun 2024 18:31:00 +0500</pubDate>
</item>
<item>
<title>When does Angular's change detection fail to work properly?</title>
<description><![CDATA[<p>Scenarios where Angular's change detection might not work as expected, specifically in projects and library components.</p>
<ol>
<li>
<p><strong>Changes in Asynchronous Operations</strong>
<br>you may need to manually trigger change detection using <code>ChangeDetectorRef.detectChanges()</code></p>
</li>
<li><strong>Immutable Objects</strong>
<br>An object where its reference does not change, but its internal state does.
You should use methods that return new instances when modifying immutable objects (e.g., Array methods like map, filter, etc., or libraries like Immutable.js, a nested object or array).
<br>ex.
<pre><code>this.agGridDynamicConfig = {... this.agGridDynamicConfig}</code></pre></li>
<li>
<p><strong>Events Outside Angular's Zone</strong>
<br> If events (like DOM events) occur outside Angular's zone. You can use <code>NgZone.run()</code> to execute code within Angular's zone.</p>
</li>
<li><strong>Third-Party Libraries</strong>
<br> If you're using third-party libraries that manipulate the DOM directly or change component state outside Angular's knowledge, you may need to wrap these operations in Angular's zone or handle change detection manually</li>
</ol>]]></description>
<link>https://sraban.github.io/post/when-does-angulars-change-detection-fail-to-work-properly</link>
<pubDate>Sat, 22 Jun 2024 19:23:00 +0500</pubDate>
</item>
<item>
<title>package.json configuration</title>
<description><![CDATA[<p>The most common fields and configurations found in package.json. Depending on the project's needs and tooling used, some of these configurations may not be present or may have additional custom configurations.</p>
<ol>
<li>
<p><strong>name</strong>: The name of the package or project.</p>
</li>
<li>
<p><strong>version</strong>: The version of the package or project, following Semantic Versioning (SemVer).</p>
</li>
<li>
<p><strong>description</strong>: A brief description of the package or project.</p>
</li>
<li>
<p><strong>keywords</strong>: An array of keywords that describe the package, useful for search optimization.</p>
</li>
<li>
<p><strong>homepage</strong>: The URL to the project's homepage.</p>
</li>
<li>
<p><strong>bugs</strong>: Information about where to report bugs for the package.</p>
</li>
<li>
<p><strong>license</strong>: The license under which the package is distributed.</p>
</li>
<li>
<p><strong>author</strong>: The name and email address of the package author.</p>
</li>
<li>
<p><strong>contributors</strong>: An array of contributors to the project.</p>
</li>
<li>
<p><strong>files</strong>: An array of files to include in the package.</p>
</li>
<li>
<p><strong>main</strong>: The entry point of the package (the main module that gets executed when the package is required).</p>
</li>
<li>
<p><strong>browser</strong>: Replacement files to use in the browser environment.</p>
</li>
<li>
<p><strong>bin</strong>: Specifies command-line commands that are installed with the package.</p>
</li>
<li>
<p><strong>scripts</strong>: An object where each key is a script name and the value is the command to run for that script.</p>
</li>
<li>
<p><strong>dependencies</strong>: An object listing the packages that the project depends on in production.</p>
</li>
<li>
<p><strong>devDependencies</strong>: An object listing packages that are only needed for development and testing.</p>
</li>
<li>
<p><strong>peerDependencies</strong>: An object listing packages that the project expects the host environment to provide.</p>
</li>
<li>
<p><strong>optionalDependencies</strong>: An object listing packages that are optional dependencies.</p>
</li>
<li>
<p><strong>engines</strong>: Specifies the versions of Node.js and npm that the package is compatible with.</p>
</li>
<li>
<p><strong>os</strong>: An array of operating systems on which the package runs.</p>
</li>
<li>
<p><strong>cpu</strong>: An array of CPUs that the package runs on.</p>
</li>
<li>
<p><strong>private</strong>: If true, prevents the package from being accidentally published to the npm registry.</p>
</li>
<li>
<p><strong>publishConfig</strong>: Configures publishing options when using npm publish.</p>
</li>
<li>
<p><strong>directories</strong>: Specifies a directory structure for the package.</p>
</li>
<li>
<p><strong>repository</strong>: The URL and type of source control repository for the package.</p>
</li>
<li>
<p><strong>scripts</strong>: Custom scripts that can be run with npm, typically used for build, test, and other automation tasks.</p>
</li>
<li>
<p><strong>config</strong>: Configuration parameters that can be used by the scripts.</p>
</li>
<li>
<p><strong>workspaces</strong>: An array of workspace packages that are part of a multi-package repository.</p>
</li>
<li>
<p><strong>jest</strong> (or other test runner configurations): Specific configurations for test runners like Jest.</p>
</li>
<li><strong>eslintConfig</strong> (or other linter configurations): Configuration for linters like ESLint.</li>
</ol>
<p>ex.</p>
<pre><code>{
  "name": "my-node-project",
  "version": "1.0.0",
  "description": "A sample Node.js project",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.17.2"
  },
  "devDependencies": {
    "jest": "^27.0.0"
  },
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/my-node-project.git"
  },
  "bugs": {
    "url": "https://github.com/your-username/my-node-project/issues"
  },
  "homepage": "https://github.com/your-username/my-node-project#readme"
}</code></pre>]]></description>
<link>https://sraban.github.io/post/packagejson-configuration</link>
<pubDate>Sat, 22 Jun 2024 18:03:00 +0500</pubDate>
</item>
<item>
<title>Watch - Production Errors & Issues,  Important Transactions from a system MQ</title>
<description><![CDATA[<p>Why need to watch the live system since resposibility is over.</p>
<p>Ans:- Before <strong>time</strong> should rectify the mistakes/errors to save from a <strong>huge loss</strong>.</p>
<p>So to track your live system in a given period of time, get the group messages about trackable events in a MQ. </p>
<p>Ex.
Some time code migrates from one server to another server, time zone changes, watch orders, verify user activity with same time, watch cron jobs are working with required time. </p>
<ul>
<li>Patch MQ to the logger to track each event</li>
<li>Event can be a server error, language/framewortk error, output of the given input</li>
</ul>]]></description>
<link>https://sraban.github.io/post/watch-production-errors-issues-important-transactions-from-a-system-mq</link>
<pubDate>Sat, 15 Jun 2024 15:42:00 +0500</pubDate>
</item>
<item>
<title>Uses of Http Guzzle PHP</title>
<description><![CDATA[<h4>About PHP Guzzle for API</h4>
<ol>
<li>
<p><code>Initialize th instance</code></p>
<pre>$this->client = new \GuzzleHttp\Client();</pre>
</li>
<li>
<p><code>Get Method</code></p>
<pre>
$params = Array(
        'key1' => $id, 
        'key2' => $value,
       );
$promise = $this->client->request('GET',  route('statement'), [
    'query' => $params,
    'debug' => false
]);

$promise->getBody()->rewind();
$output = $promise->getBody()->getContents();
$httpStatusCode = $promise->getStatusCode();
</pre>
</li>
<li>
<p><code>Post Method: Raw data posting like posting json in payload</code></p>
<pre>
$rawInput = json_encode($array); // string

-OR-

$rawInput = <<<EOF
        SET empdata 2 ‘Jack Petter’ ‘192.168.11.11’
        GET empdata ‘192.168.11.11’
        SET empwebhistory 192.168.11.11 ‘http://google.com’
        SET empwebhistory 192.168.11.11 ‘http://facebook.com’
        GET empwebhistory  192.168.11.11
        UNSET empwebhistory  192.168.11.11
        GET empwebhistory 192.168.11.11
        END
    EOF;

$promise = $this->client->request('POST',  route('statement'), [
    'body' => $rawInput,
    'debug' => false
]);

$promise->getBody()->rewind();
$output = $promise->getBody()->getContents();
$httpStatusCode = $promise->getStatusCode();
</pre>
</li>
<li>
<p><code>Post using Form Fields</code></p>
<pre>
$filedInputs = Array(
    'email' => 'test@gmail.com',
    'name' => 'Test user',
    'password' => 'testpassword',
);

$promise = $this->client->request('POST',  route('statement'), [
    'form_params' => $rawInput,
    'debug' => false
]);

$promise->getBody()->rewind();
$output = $promise->getBody()->getContents();
$httpStatusCode = $promise->getStatusCode();
</pre>
</li>
<li>
<p><code>POST File Upload from Directory</code></p>
<pre>
$promise = $this->client->request('POST', 'http://www.example.com/files/post', [
    'multipart' => [
        [
            'name'     => 'file_name',
            'contents' => fopen('/path/to/file', 'r')
        ],
        [
            'name'     => 'csv_header',
            'contents' => 'First Name, Last Name, Username',
            'filename' => 'csv_header.csv'
        ]
    ]
]);

$promise->getBody()->rewind();
$output = $promise->getBody()->getContents();
$httpStatusCode = $promise->getStatusCode();
</pre>
</li>
</ol>]]></description>
<link>https://sraban.github.io/post/uses-of-http-guzzle-php</link>
<pubDate>Fri, 24 Apr 2020 14:42:00 +0500</pubDate>
</item>
<item>
<title>API Rate-Limiting</title>
<description><![CDATA[<p><strong>Implementing API Rate-Limiting</strong></p>
<ol>
<li>
<p>Request Queues:</p>
<ul>
<li>Ex. ASQS(Amazon Simple Queue Service) is a readymade request queue library maintianed regularly.</li>
<li>Some library take 2 request per second from the queue and remaining kept in the queue for processing.</li>
<li>Many custom &quot;request-rate-limiter&quot; library are avaialbe in package manager</li>
</ul>
</li>
<li>
<p>Throttling</p>
<ul>
<li>API is used by setting up a temporary state, allowing the API to assess each request. When the throttle is triggered, a user may either be disconnected or simply have their bandwidth reduced.</li>
<li>Possible at the application, API, or user level, throttling is a popular method to rate-limit APIs</li>
</ul>
</li>
<li>Rate-limiting Algorithms
<ul>
<li>&quot;Leaky Bucket&quot; is a algorithim which translate requests into FIFO format and processing the items on the queue on a regular rate.</li>
<li>&quot;Sliding Log&quot; is time stampped algorithim, the sum of the logs are calculated to determine the request rate. if the request exceeds the limit threshold, they are simply queued.</li>
</ul></li>
</ol>]]></description>
<link>https://sraban.github.io/post/api-rate-limiting</link>
<pubDate>Tue, 14 Apr 2020 18:17:00 +0500</pubDate>
</item>
<item>
<title>Most Useful Linux Commands</title>
<description><![CDATA[<ul>
<li>
<p><strong>uname</strong> command is simple tool which provides information about the kernel, machine, processor, and operating system</p>
<p><code>uname -m</code></p>
</li>
<li>
<p>check ubuntu linux version</p>
<p><code>lsb_release -a</code></p>
</li>
<li>
<p>show the amount of free disk space on each mounted disk</p>
<p><code>df -h</code></p>
</li>
<li>
<p>displays the total amount of free space available along with the amount of memory used and swap memory in the system</p>
<p><code>free --human</code></p>
</li>
<li>
<p>The grep filter searches a file for a particular pattern of characters, and displays all lines that contain that pattern</p>
<p><code>grep -iRl "your-text-to-find" ./</code></p>
</li>
<li>
<p>displays network connections for Transmission Control Protocol</p>
<p><code>netstat -tlnp</code></p>
</li>
<li>
<p>show all process even the current process is not associated with any TTY (terminal)</p>
<p><code>ps ax | grep docker</code></p>
</li>
<li>
<p>Watching activity on Linux with watch and tail commands </p>
<p><code>watch "dmesg | tail -20"</code></p>
</li>
</ul>]]></description>
<link>https://sraban.github.io/post/most-useful-linux-commands</link>
<pubDate>Tue, 14 Apr 2020 18:09:00 +0500</pubDate>
</item>
<item>
<title>Restful Services</title>
<description><![CDATA[<p><strong># Why REST</strong> </p>
<ol>
<li>Rest can be fetched from cahced storage instead of always heating to Server</li>
<li>Rest is Stateless.</li>
<li>Rest can work in very low bandwidth</li>
<li>REST request &amp; response can be in  TEXT, XML, HTML and JSON Format.</li>
<li>REST is an architectural pattern not a protocal.</li>
<li>REST can make use of SOAP services.</li>
<li>Challenges in Rest - Lack of Security &amp; Lack of state</li>
</ol>
<p><strong># Methods:</strong></p>
<ol>
<li>
<p>GET</p>
<ul>
<li>This method is like calling a uri with query parameters.</li>
</ul>
</li>
<li>
<p>POST</p>
<ul>
<li>Not idempotent method, used to <strong>create</strong> records in DB</li>
<li>Since record does not exits before, save url and return url is different.</li>
</ul>
</li>
<li>
<p>PUT</p>
<ul>
<li>This method is idempotent. means client and server urls are same.</li>
<li>Used for updating a resource by using the primary key.</li>
<li>This method specifiy to fully update the db record with the variables.</li>
<li>When the the url with primary key is not valid, the record is auto generated in database.</li>
</ul>
</li>
<li>
<p>PATCH</p>
<ul>
<li>This method is idempotent. means client and server urls are same.</li>
<li>Used for updating a resource by using the primary key.</li>
<li>This method specifiy to partially update the db record.</li>
<li>When the the url with primary key is not valid, the record is auto generated in database.</li>
</ul>
</li>
<li>DELETE
<ul>
<li>This method in REST is used to destroy the resource. </li>
<li>Destroyed resource are not called again To DELETE the resource </li>
</ul></li>
</ol>]]></description>
<link>https://sraban.github.io/post/restful-services</link>
<pubDate>Tue, 14 Apr 2020 17:53:00 +0500</pubDate>
</item>
<item>
<title>Upload Large Files to server using script</title>
<description><![CDATA[<p><strong>Steps</strong></p>
<ol>
<li>
<p>Choose a javascript plugin which devide the binary data into equal multiple chunks from client.</p>
</li>
<li>
<p>Pass to server each chunk of binary data with file information. ex. mdf5 name, total chunks, processed chunk in each ajax request one by one to write/append  in same file on server.</p>
</li>
<li>when the total chunks and chunk count in ajax request is same, then file appending/uploading will complete in server, returning a message to clinet.</li>
</ol>]]></description>
<link>https://sraban.github.io/post/upload-large-files-to-server-using-script</link>
<pubDate>Tue, 14 Apr 2020 17:51:00 +0500</pubDate>
</item>
<item>
<title>Optimising MySQL Query</title>
<description><![CDATA[<p><strong>Steps</strong></p>
<ol>
<li>Expain in select query will produce the number of comparison done, look at the column to assign indexing to column</li>
<li>Remove generalised statements, instead specifiy the required columns</li>
<li>Remove in() and not In() from where clause rather use =. !=</li>
<li>Never use subquery if possible, rather to use join</li>
<li>De-normalize when not necessary to devide tables futher on basis of normalisation.</li>
</ol>]]></description>
<link>https://sraban.github.io/post/optimising-mysql-query</link>
<pubDate>Tue, 14 Apr 2020 17:50:00 +0500</pubDate>
</item>
<item>
<title>Supervisor & Crontabs</title>
<description><![CDATA[<p><strong>Supervisor (Running Program as a process)</strong></p>
<p>When commad stops after closing the shell terminal, Supervisor takes care to run the command in background as process, Whenever required the process can be killed on shell.</p>
<p>Steps to run the angular development server in background</p>
<ol>
<li>sudo apt-get install supervisor</li>
<li>vim /etc/supervisor/conf.d/ngServe.conf</li>
<li>supervisorctl</li>
<li>vim /var/log/ngServe.out.log</li>
<li>tail -f /var/log/ngServe.out.log</li>
<li>ps ax | grep PID(4098)</li>
</ol>
<p><strong>inside the file &quot;ngServe.conf&quot;</strong></p>
<ol>
<li>[program:ngServe]</li>
<li>command=cd /var/www/html/ng &amp;&amp; ng serve --port=8088</li>
<li>autostart=true</li>
<li>autorestart=true</li>
<li>stderr_logfil=/var/log/ngServe.out.log</li>
<li>stdout_logfile=/var/log/ngServe.out.log</li>
</ol>
<hr />
<p><strong>Crontab examples</strong></p>
<ol>
<li>crontab -e</li>
<li>Examples
<ul>
<li>0 5 <em> </em> 1 tar -zcf /var/backups/home.tgz /home/</li>
<li>0 5 <em> </em> 1 php  /var/www/html/run.php</li>
<li>0 5 <em> </em> 1 php  php artisan comission:monthly</li>
</ul></li>
</ol>]]></description>
<link>https://sraban.github.io/post/supervisor-crontabs</link>
<pubDate>Tue, 14 Apr 2020 17:49:00 +0500</pubDate>
</item>
<item>
<title>Design Patterns</title>
<description><![CDATA[<p>In software engineering, a design pattern is a general, reusable solution to a commonly occurring problem within a given context in software design.</p>
<p><strong>Some Of Design Patterns:</strong></p>
<p><strong>Singleton</strong></p>
<ul>
<li>Whenever only dingle instance of a object is required through out the system</li>
<li>Example  DB connection &amp; Logger writing Object.</li>
<li>In Angular, any services with @injectable() are singleton always. </li>
</ul>
<p><strong>Facade</strong></p>
<ul>
<li>A facade is an object that serves as a front-facing interface masking more complex underlying or structural code</li>
<li>Provide a unified interface to a set of interfaces in a subsystem.</li>
<li>Facade deals with interfaces, not implementation. Its purpose is to hide internal complexity behind a single interface that appears simple on the outside.</li>
</ul>
<p><strong>Adaptor</strong></p>
<ul>
<li>The Adapter pattern allows otherwise incompatible classes to work together by converting the interface of one class into an interface expected by the clients</li>
<li>It allows two or more previously incompatible objects to interact.</li>
<li>It allows reusability of existing functionality.</li>
<li>For example, sharing post in diferent social medias like fb,tweet,linkedin can be done using one interface implements the common methods in different classes of social media to share the data. </li>
</ul>
<p><strong>Observable</strong></p>
<ul>
<li>An Observer Pattern says that &quot;just define a one-to-one dependency so that when one object changes state, all its dependents are notified and updated automatically&quot;.</li>
<li>It describes the coupling between the objects and the observer.</li>
<li>It provides the support for broadcast-type communication.</li>
<li>When the change of a state in one object must be reflected in another object without keeping the objects tight coupled.</li>
</ul>
<p><strong>Proxy</strong></p>
<ul>
<li>Simply, proxy means an object representing another object</li>
<li>It provides the protection to the original object from the outside world.</li>
<li>So, we can perform many operations like hiding the information of original object, on demand loading etc.</li>
<li>A proxy, in its most general form, is a class functioning as an interface to something else.</li>
<li>In proxy pattern, we create object having original object to interface its functionality to outer world.</li>
</ul>
<p><strong>Front Controller</strong></p>
<ul>
<li>Where single entry point like index.php, index.html inframeworks</li>
</ul>
<p><strong>Factory</strong></p>
<ul>
<li>Factory pattern helps in instantiation and creation of Objects</li>
<li>According to definition from wikipedia, Factory Pattern is &quot;A factory is an object for creating other objects&quot;</li>
<li>In simple factory pattern, we have a factory class which has a method that returns different types of object based on given input</li>
<li>For an example a Factory produces different type of fans, and all type of fan are kept for Testing, instead of swtiching on / off each fan, a factory function should be generated which will test the fan by passing the type of fan, no need to go detail to check how fans are switched off and switched on. It is kind of abstracting the deail functionality of various fans(table fan, ceiling fan, powered fan, wall fan).</li>
<li>In this pattern, a class simply creates the object you want to use. Consider the following example of the factory pattern:</li>
</ul>
<pre>
<code>
class Automobile
{
    private $vehicleMake;
    private $vehicleModel;

    public function __construct($make, $model)
    {
        $this->vehicleMake = $make;
        $this->vehicleModel = $model;
    }

    public function getMakeAndModel()
    {
        return $this->vehicleMake . ' ' . $this->vehicleModel;
    }
}

class AutomobileFactory
{
    public static function create($make, $model)
    {
        return new Automobile($make, $model);
    }
}

// have the factory create the Automobile object
$veyron = AutomobileFactory::create('Bugatti', 'Veyron');

print_r($veyron->getMakeAndModel()); // outputs "Bugatti Veyron"

</code>
</pre>
<p><strong>Strategy Pattern</strong></p>
<ul>
<li>With the strategy pattern you encapsulate specific families of algorithms allowing the client class responsible for instantiating a particular algorithm to have no knowledge of the actual implementation. This first code snippet outlines a family of algorithms; you may want a serialized array, some JSON or maybe just an array of data</li>
</ul>
<pre>
<code>
interface OutputInterface
{
    public function load();
}

class SerializedArrayOutput implements OutputInterface
{
    public function load()
    {
        return serialize($arrayOfData);
    }
}

class JsonStringOutput implements OutputInterface
{
    public function load()
    {
        return json_encode($arrayOfData);
    }
}

class ArrayOutput implements OutputInterface
{
    public function load()
    {
        return $arrayOfData;
    }
}

class SomeClient
{
    private $output;

    public function setOutput(OutputInterface $outputType)
    {
        $this->output = $outputType;
    }

    public function loadOutput()
    {
        return $this->output->load();
    }
}

// Want some JSON?
$client->setOutput(new JsonStringOutput());
$data = $client->loadOutput();
</code>
</pre>]]></description>
<link>https://sraban.github.io/post/design-patterns</link>
<pubDate>Tue, 14 Apr 2020 17:46:00 +0500</pubDate>
</item>
<item>
<title>Optimising PHP Scripts</title>
<description><![CDATA[<p><em>Mainly to avoid generalised coding for better usage of memory and time, use specific and proper logic, statement, inbuilt function.</em></p>
<ol>
<li>Take advantage of native php function</li>
<li>Cut out unnecessary classes</li>
<li>Turn off debugging notification</li>
<li>close file and databse connections</li>
<li>unnessary loop needs to breaked. unsetting memories after usages of variable and object.</li>
<li>Use single quote rather double quote</li>
<li>use identify operator ===</li>
<li>use proper string function</li>
<li>calculate only once</li>
<li>Pass reference to the function to use the veriable outside the function</li>
<li>Reduce number of hits to the database</li>
<li>Use intermidate caching technology rather processing same thing again</li>
<li>Use derived methods instead of base methods in class</li>
<li>use json rather XML for data interchange</li>
<li>use isset() always rather using variables directly</li>
</ol>]]></description>
<link>https://sraban.github.io/post/optimising-php-scripts</link>
<pubDate>Tue, 14 Apr 2020 14:50:00 +0500</pubDate>
</item>
</channel>
</rss>
